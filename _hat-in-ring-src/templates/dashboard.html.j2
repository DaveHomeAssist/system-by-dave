<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>2028 Hat-in-Ring Radar</title>
<meta name="description" content="{{ page_desc }}">
<link rel="canonical" href="{{ canonical_url }}">
<meta name="theme-color" content="#1a1a1a">
<meta name="robots" content="index,follow">
<meta property="og:type" content="website">
<meta property="og:site_name" content="System by Dave">
<meta property="og:title" content="2028 Hat-in-Ring Radar — presidential signal tracker">
<meta property="og:description" content="{{ page_desc }}">
<meta property="og:url" content="{{ canonical_url }}">
<meta property="og:image" content="{{ og_image }}">
<meta name="twitter:card" content="summary_large_image">
<meta name="twitter:title" content="2028 Hat-in-Ring Radar">
<meta name="twitter:description" content="{{ page_desc }}">
<meta name="twitter:image" content="{{ og_image }}">
<script type="application/ld+json">{"@context":"https://schema.org","@type":"WebSite","name":"2028 Hat-in-Ring Radar","url":"{{ canonical_url }}","description":"{{ page_desc }}","publisher":{"@type":"Organization","name":"System by Dave","url":"https://systembydave.com/"}}</script>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Pinyon+Script&family=Space+Grotesk:wght@400;500;600;700&display=swap" rel="stylesheet">
<style>
:root{
  color-scheme: light;
  --bg:#f6f5f1; --panel:#ffffff; --ink:#1a1a1a; --muted:#6b6b6b; --line:#e3e1da;
  --dem:#2f6fd1; --rep:#d23b3b; --lib:#c79a18; --ind:#3b9c8a; --other:#7a7a7a;
  --t5:#c0392b; --t4:#e07a1f; --t3:#d4a017; --t2:#2f6fd1; --t1:#7d8794; --t0:#aab0b6;
  --good:#1f9d55; --bad:#c0392b;
  --shadow:0 1px 2px rgba(20,20,20,.05),0 1px 1px rgba(20,20,20,.04);
}
*{box-sizing:border-box}
body{margin:0;background:var(--bg);color:var(--ink);
  font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,Helvetica,Arial,sans-serif;
  font-size:14px;line-height:1.45;-webkit-font-smoothing:antialiased}
h1,h2,h3{font-family:Georgia,"Times New Roman",serif;font-weight:600;letter-spacing:-.01em;margin:0}
a{color:var(--dem);text-decoration:none}
.wrap{max-width:1180px;margin:0 auto;padding:22px 20px 60px}
header.top{display:flex;flex-wrap:wrap;align-items:flex-end;gap:12px;justify-content:space-between;
  border-bottom:2px solid var(--ink);padding-bottom:14px;margin-bottom:18px}
.brand h1{font-size:30px;line-height:1.05}
.brand .sub{color:var(--muted);font-size:12.5px;margin-top:4px;letter-spacing:.02em;text-transform:uppercase}
.asof{font-size:12px;color:var(--muted);text-align:right}
.asof b{color:var(--ink)}
.asof .buildstamp{display:inline-block;margin-top:3px;font-size:11px;opacity:.85}
.asof .buildstamp::before{content:"⟳ ";font-weight:600}
.stats{display:grid;grid-template-columns:repeat(6,1fr);gap:10px;margin-bottom:18px}
.stat{background:var(--panel);border:1px solid var(--line);border-radius:10px;padding:11px 12px;box-shadow:var(--shadow)}
.stat .n{font-family:Georgia,serif;font-size:24px;font-weight:600;line-height:1}
.stat .l{font-size:10.5px;color:var(--muted);text-transform:uppercase;letter-spacing:.04em;margin-top:5px}
.stat .dot{display:inline-block;width:8px;height:8px;border-radius:50%;margin-right:5px;vertical-align:1px}
.toolbar{position:sticky;top:0;z-index:20;background:var(--bg);padding:10px 0;margin-bottom:6px;
  border-bottom:1px solid var(--line);display:flex;flex-wrap:wrap;gap:8px;align-items:center}
.toolbar .grow{flex:1 1 180px}
input[type=search],select{font:inherit;padding:7px 9px;border:1px solid var(--line);border-radius:8px;background:#fff;color:var(--ink)}
input[type=search]{width:100%}
.chips{display:flex;flex-wrap:wrap;gap:6px}
.chip{border:1px solid var(--line);background:#fff;border-radius:999px;padding:5px 11px;font-size:12.5px;cursor:pointer;color:var(--muted);user-select:none}
.chip.on{background:var(--ink);color:#fff;border-color:var(--ink)}
.chip.party.on[data-v=Democrat]{background:var(--dem);border-color:var(--dem)}
.chip.party.on[data-v=Republican]{background:var(--rep);border-color:var(--rep)}
.chip.party.on[data-v=Libertarian]{background:var(--lib);border-color:var(--lib)}
.chip.party.on[data-v=Independent]{background:var(--ind);border-color:var(--ind)}
.btn{border:1px solid var(--line);background:#fff;border-radius:8px;padding:7px 11px;font:inherit;cursor:pointer;color:var(--ink)}
.btn:hover{background:#faf9f6}
.btn.primary{background:var(--ink);color:#fff;border-color:var(--ink)}
.seg{display:flex;border:1px solid var(--line);border-radius:8px;overflow:hidden}
.seg button{border:0;background:#fff;padding:7px 12px;font:inherit;cursor:pointer;color:var(--muted)}
.seg button.on{background:var(--ink);color:#fff}
.layout{display:grid;grid-template-columns:1fr 320px;gap:20px;margin-top:14px;align-items:start}
@media(max-width:900px){.layout{grid-template-columns:1fr}.stats{grid-template-columns:repeat(3,1fr)}}
.panel{background:var(--panel);border:1px solid var(--line);border-radius:12px;box-shadow:var(--shadow)}
.panel h2{font-size:15px;padding:13px 15px;border-bottom:1px solid var(--line)}
.panel h2 .meta{float:right;font-family:inherit;font-weight:400;font-size:11.5px;color:var(--muted);text-transform:none}
/* leaderboard */
table{width:100%;border-collapse:collapse}
th,td{text-align:left;padding:9px 12px;border-bottom:1px solid var(--line);font-size:13px;vertical-align:middle}
th{font-size:11px;text-transform:uppercase;letter-spacing:.04em;color:var(--muted);cursor:pointer;white-space:nowrap;background:#fbfaf7}
th.sorted::after{content:" ▾";color:var(--ink)}
th.sorted.asc::after{content:" ▴"}
tr.row{cursor:pointer}
tr.row:hover{background:#fbfaf7}
.rank{color:var(--muted);font-variant-numeric:tabular-nums;width:26px}
.nm{font-weight:600}
.nm .role{display:block;font-weight:400;color:var(--muted);font-size:11.5px}
.pbar{display:inline-block;width:3px;height:26px;border-radius:2px;margin-right:9px;vertical-align:middle}
.tier{display:inline-block;font-size:11px;font-weight:600;padding:2px 8px;border-radius:999px;color:#fff;white-space:nowrap}
.scoreCell{font-variant-numeric:tabular-nums;font-weight:600;width:120px}
.track{display:inline-block;width:62px;height:7px;background:#eee;border-radius:4px;overflow:hidden;margin-left:8px;vertical-align:middle}
.track > i{display:block;height:100%;border-radius:4px}
.delta{font-variant-numeric:tabular-nums;font-weight:600;font-size:12px}
.delta.up{color:var(--good)} .delta.dn{color:var(--bad)} .delta.fl{color:var(--muted)}
.conf{font-size:11px;color:var(--muted)}
.dotc{display:inline-block;width:7px;height:7px;border-radius:50%;margin-right:4px;vertical-align:1px}
/* cards */
.cards{display:grid;grid-template-columns:repeat(auto-fill,minmax(248px,1fr));gap:12px;padding:14px}
.card{border:1px solid var(--line);border-radius:11px;padding:13px;background:#fff;cursor:pointer;transition:.12s;position:relative;overflow:hidden}
.card:hover{border-color:#c9c6bd;transform:translateY(-1px)}
.card .accent{position:absolute;left:0;top:0;bottom:0;width:4px}
.card .ch{display:flex;justify-content:space-between;align-items:flex-start;gap:8px;margin-left:6px}
.card .nm2{font-weight:600;font-size:15px}
.card .role2{color:var(--muted);font-size:11.5px}
.card .sig{margin:9px 0 8px 6px;font-size:12.5px;color:#333}
.card .foot{display:flex;justify-content:space-between;align-items:center;margin-left:6px;margin-top:8px}
.avatar{width:44px;height:44px;border-radius:9px;object-fit:cover;object-position:center top;flex:none;background:#ece9e2;border:1px solid var(--line)}
.avatarSm{width:26px;height:26px;border-radius:6px;object-fit:cover;object-position:center top;vertical-align:middle;margin-right:8px;background:#ece9e2;border:1px solid var(--line)}
.dhPortrait{width:62px;height:62px;border-radius:10px;object-fit:cover;object-position:center top;float:right;margin:0 0 8px 12px;background:#ece9e2;border:1px solid var(--line)}
.scoreBig{font-family:Georgia,serif;font-size:21px;font-weight:600}
/* feed */
.feed{padding:6px 0}
.feed .it{padding:10px 15px;border-bottom:1px solid var(--line)}
.feed .it:last-child{border-bottom:0}
.feed .d{font-size:11px;color:var(--muted);text-transform:uppercase;letter-spacing:.03em}
.feed .h{font-size:12.5px;margin-top:3px}
.feed .h b{font-weight:600}
.legend{padding:12px 15px;font-size:12px;color:var(--muted)}
.legend .li{display:flex;gap:8px;align-items:center;margin:6px 0}
.legend .sw{width:26px;flex:0 0 26px;text-align:center}
/* drawer */
.scrim{position:fixed;inset:0;background:rgba(20,20,20,.34);opacity:0;pointer-events:none;transition:.18s;z-index:40}
.scrim.on{opacity:1;pointer-events:auto}
.drawer{position:fixed;top:0;right:0;height:100%;width:430px;max-width:94vw;background:#fff;box-shadow:-8px 0 30px rgba(0,0,0,.16);
  transform:translateX(100%);transition:.22s cubic-bezier(.4,0,.2,1);z-index:50;overflow-y:auto}
.drawer.on{transform:none}
.drawer .dh{padding:18px 20px;border-bottom:1px solid var(--line);position:sticky;top:0;background:#fff;z-index:2}
.drawer .dh .x{float:right;cursor:pointer;font-size:20px;color:var(--muted);line-height:1}
.drawer .body{padding:18px 20px}
.kv{display:grid;grid-template-columns:120px 1fr;gap:6px 12px;font-size:13px;margin:14px 0}
.kv .k{color:var(--muted)}
.quote{border-left:3px solid var(--line);padding:6px 0 6px 13px;color:#333;font-style:italic;margin:12px 0;font-size:13.5px}
.break{margin-top:16px}
.break h4{margin:0 0 7px;font-size:11.5px;text-transform:uppercase;letter-spacing:.04em;color:var(--muted);font-family:inherit;font-weight:700}
.cmp{display:flex;justify-content:space-between;padding:6px 0;border-bottom:1px dashed var(--line);font-size:13px}
.cmp .v{font-variant-numeric:tabular-nums;font-weight:600}
.cmp .v.pos{color:var(--good)} .cmp .v.neg{color:var(--bad)}
.cmp.total{border-bottom:0;border-top:2px solid var(--ink);margin-top:4px;padding-top:8px;font-weight:700}
.tagrow{display:flex;flex-wrap:wrap;gap:6px;margin-top:10px}
.tag{font-size:11px;background:#f1efe9;border-radius:5px;padding:3px 7px;color:#555}
/* edit form */
.form label{display:block;font-size:12px;color:var(--muted);margin:10px 0 3px}
.form input[type=text],.form textarea,.form select{width:100%;font:inherit;padding:7px 9px;border:1px solid var(--line);border-radius:7px}
.form textarea{resize:vertical;min-height:46px}
.checks{display:grid;grid-template-columns:1fr 1fr;gap:4px 12px;margin-top:6px}
.checks label{display:flex;align-items:center;gap:6px;font-size:12.5px;color:var(--ink);margin:2px 0}
.livescore{background:#faf9f6;border:1px solid var(--line);border-radius:8px;padding:10px 12px;margin:14px 0;display:flex;justify-content:space-between;align-items:center}
.note{font-size:11.5px;color:var(--muted);background:#faf9f6;border:1px solid var(--line);border-radius:8px;padding:9px 11px;margin-top:8px}
footer{margin-top:26px;padding-top:14px;border-top:1px solid var(--line);font-size:11.5px;color:var(--muted)}
.hide{display:none!important}
.pollLead{font-size:10px;background:#fff3cd;color:#8a6d00;border:1px solid #f0e0a0;border-radius:5px;padding:1px 6px;margin-left:6px;font-weight:600}
.segbadge{display:inline-block;min-width:16px;padding:0 5px;margin-left:2px;border-radius:9px;background:var(--rep);color:#fff;font-size:10px;font-weight:700;line-height:16px;text-align:center}
.segbadge:empty{display:none}
.reviewNote{font-size:12px;color:var(--muted);background:#faf9f6;border:1px solid var(--line);border-radius:8px;padding:10px 12px;margin-bottom:12px}
.reviewNote code{background:#eee;padding:1px 4px;border-radius:4px;font-size:11px}
.reviewItem{border:1px solid var(--line);border-radius:10px;padding:11px 13px;margin-bottom:10px;background:#fff}
.ri-head{display:flex;align-items:center;gap:8px;flex-wrap:wrap}
.ri-src{color:var(--muted);font-size:11.5px;margin-left:auto}
.ri-headline{margin:6px 0 8px;font-size:13.5px;line-height:1.45}
.ri-keys{display:flex;gap:5px;flex-wrap:wrap;margin-bottom:9px}
.kchip{font-size:10.5px;background:#eef;color:#33438a;border:1px solid #dfe3f5;border-radius:5px;padding:1px 6px;font-weight:600}
.ri-act{display:flex;align-items:center;gap:8px}
.ri-link{font-size:11.5px;color:var(--muted);margin-left:auto;text-decoration:none}
.kind{font-size:10px;font-weight:700;border-radius:5px;padding:1px 6px;text-transform:uppercase;letter-spacing:.02em}
.kind.disc{background:#e7f1ff;color:#1f5fb0}
.kind.den{background:#ffe8e8;color:#b22}
.kind.fec{background:#ece7ff;color:#5a3fb0}
.reviewEmpty{color:var(--muted);text-align:center;padding:26px 0;font-size:13px}
.reviewActions{margin-top:6px;display:flex;align-items:center;gap:10px}
/* ---- anticipatory UX + microinteractions ---- */
:root{--ease-out:cubic-bezier(.2,0,.2,1);--ease-standard:cubic-bezier(.4,0,.2,1)}
@media (prefers-reduced-motion: no-preference){
  .btn,.seg button,.chip,.qchip{transition:transform .12s var(--ease-out),background .12s var(--ease-out),box-shadow .12s var(--ease-out)}
  .btn:active,.seg button:active,.chip:active,.qchip:active{transform:scale(.97)}
  tr.row{transition:background .12s var(--ease-out)}
  .pop{animation:pop .15s var(--ease-out)} @keyframes pop{50%{transform:scale(1.06)}}
  .shake{animation:shake .18s ease-in-out} @keyframes shake{10%,90%{transform:translateX(-4px)}30%,70%{transform:translateX(4px)}50%{transform:translateX(-2px)}}
  .flash{animation:flash .6s var(--ease-out)} @keyframes flash{from{background:#dff5e6}to{background:transparent}}
  .fadein{animation:fadein .14s var(--ease-out)} @keyframes fadein{from{opacity:0}}
  th.sorted::after{display:inline-block;transition:transform .12s var(--ease-out)}
}
@media (prefers-reduced-motion: reduce){*{animation-duration:.001ms!important;transition-duration:.001ms!important}}
.toast{position:fixed;left:50%;bottom:24px;transform:translateX(-50%) translateY(8px);background:var(--ink);color:#fff;
  font-size:13px;padding:9px 16px;border-radius:9px;box-shadow:0 4px 14px rgba(0,0,0,.18);opacity:0;pointer-events:none;
  z-index:60;transition:opacity .16s var(--ease-out),transform .16s var(--ease-out)}
.toast.on{opacity:1;transform:translateX(-50%) translateY(0)}
.toast.err{background:var(--bad)}
.f-err{outline:2px solid var(--bad);outline-offset:1px;border-radius:8px}
.segbadge.alt{background:var(--good)}
.newdot{display:inline-block;width:6px;height:6px;border-radius:50%;background:var(--good);margin-left:6px;vertical-align:1px}
.quickbar{display:flex;gap:6px;flex-wrap:wrap;margin:2px 0 12px}
.qchip{border:1px solid var(--line);background:#fff;border-radius:999px;padding:5px 12px;font-size:12px;cursor:pointer;color:var(--muted);user-select:none;min-height:30px}
.qchip.on{background:var(--ink);color:#fff;border-color:var(--ink)}
.qchip .qn{opacity:.65;margin-left:5px;font-variant-numeric:tabular-nums}
.nudge{display:flex;align-items:center;gap:10px;background:#eef5ff;border:1px solid #d4e3fb;color:#274b78;
  border-radius:10px;padding:9px 13px;margin-bottom:14px;font-size:12.5px}
.nudge .x{cursor:pointer;color:#274b78;opacity:.6;font-size:16px;line-height:1}
.resetlink{cursor:pointer;color:var(--muted);text-decoration:underline}
/* ---- v2: trajectory, early-state board, compare, money, share, a11y ---- */
.spark{vertical-align:middle;display:inline-block}
.trendCell{width:90px;white-space:nowrap}
.slopen{font-variant-numeric:tabular-nums;font-size:11px;margin-left:5px}
.slopen.up{color:var(--good)} .slopen.dn{color:var(--bad)} .slopen.fl{color:var(--muted)}
.flags{display:flex;flex-wrap:wrap;gap:4px;margin:7px 0 0 6px}
.flag{font-size:9.5px;font-weight:700;letter-spacing:.03em;border-radius:5px;padding:1px 6px;text-transform:uppercase;border:1px solid transparent}
.flag.new{background:#e6f6ec;color:#1f7a44;border-color:#bfe6cd}
.flag.hot{background:#fdecea;color:#c0392b;border-color:#f3c4bd}
.flag.cool{background:#eef2f7;color:#5b6b7d;border-color:#d3dbe5}
.flag.stale{background:#f3f1ec;color:#8a8475;border-color:#e0dccf}
.flag.verified{background:#eef7f1;color:#2c7a52;border-color:#cfe7d8}
/* crawl summary (SEO / no-JS) */
.crawl-summary{font-size:12.5px;color:var(--muted);margin:0 0 14px}
.crawl-summary noscript table{width:100%;margin-top:8px}
/* briefing */
.briefing .b-item{display:flex;justify-content:space-between;gap:8px;padding:7px 15px;border-bottom:1px solid var(--line);font-size:12.5px;cursor:pointer}
.briefing .b-item:last-child{border-bottom:0}
.briefing .b-empty{padding:12px 15px;color:var(--muted);font-size:12.5px}
.briefing .b-sec{font-size:10.5px;text-transform:uppercase;letter-spacing:.04em;color:var(--muted);padding:9px 15px 3px;font-weight:700}
.b-d{font-variant-numeric:tabular-nums;font-weight:600}
.b-d.up{color:var(--good)} .b-d.dn{color:var(--bad)} .b-d.fl{color:var(--muted)}
.b-share{padding:10px 15px;border-top:1px solid var(--line);font-size:12px}
/* trajectory view */
.trajCtl{display:flex;gap:8px;align-items:center;padding:10px 15px;border-bottom:1px solid var(--line);font-size:12.5px;color:var(--muted)}
/* early-state big board */
.bigboard{display:grid;grid-template-columns:repeat(2,1fr);gap:12px;padding:14px}
@media(max-width:560px){.bigboard{grid-template-columns:1fr}}
.statecard{border:1px solid var(--line);border-radius:11px;padding:13px;background:#fff;cursor:pointer;transition:.12s}
.statecard:hover,.statecard:focus-visible{border-color:#c9c6bd;transform:translateY(-1px);outline:none}
.statecard h3{font-size:16px;display:flex;justify-content:space-between;align-items:baseline}
.statecard .sc-n{font-family:Georgia,serif;font-size:22px;color:var(--dem)}
.heatbar{height:8px;border-radius:4px;background:#eee;overflow:hidden;margin:9px 0}
.heatbar > i{display:block;height:100%;background:linear-gradient(90deg,var(--t2),var(--t4))}
.sc-list{font-size:12.5px;color:#333;margin-top:6px}
.sc-list .sc-row{display:flex;justify-content:space-between;padding:3px 0;border-bottom:1px dashed var(--line)}
.sc-row:last-child{border-bottom:0}
.statefilter{display:flex;align-items:center;gap:8px;background:#eef5ff;border:1px solid #d4e3fb;color:#274b78;border-radius:8px;padding:7px 12px;margin:0 0 12px;font-size:12.5px}
.statefilter .x{cursor:pointer;margin-left:auto;font-size:16px;line-height:1}
/* compare */
.cmp-pick{position:absolute;top:9px;right:9px;width:18px;height:18px;cursor:pointer;z-index:2}
.cmptray{position:fixed;left:50%;bottom:18px;transform:translateX(-50%);background:var(--ink);color:#fff;border-radius:12px;
  padding:9px 14px;display:flex;gap:10px;align-items:center;box-shadow:0 6px 22px rgba(0,0,0,.2);z-index:55;font-size:12.5px;max-width:94vw}
.cmptray .cchip{background:rgba(255,255,255,.16);border-radius:7px;padding:3px 8px;display:inline-flex;gap:6px;align-items:center}
.cmptray .cchip .x{cursor:pointer;opacity:.7}
.cmpgrid{display:grid;gap:12px;padding:14px;grid-template-columns:repeat(auto-fit,minmax(220px,1fr))}
.cmpcol{border:1px solid var(--line);border-radius:11px;padding:13px;background:#fff}
.cmpcol h3{font-size:15px}
.cmpcol .crow{display:flex;justify-content:space-between;gap:8px;padding:6px 0;border-bottom:1px dashed var(--line);font-size:12.5px}
.cmpcol .crow:last-child{border-bottom:0}
.cmpcol .crow .ck{color:var(--muted)}
.cmpEmpty{padding:26px 16px;color:var(--muted);text-align:center;font-size:13px}
/* money */
.money-tbl{width:100%;font-size:12.5px;margin-top:4px}
.money-tbl td{padding:4px 0;border-bottom:1px dashed var(--line)}
.money-tbl td:last-child{text-align:right;font-variant-numeric:tabular-nums;font-weight:600}
.money-na{color:var(--muted);font-size:12.5px}
.moneyCell{font-variant-numeric:tabular-nums;font-size:12px;white-space:nowrap}
/* a11y: visible focus everywhere */
a:focus-visible,button:focus-visible,select:focus-visible,input:focus-visible,
.chip:focus-visible,.qchip:focus-visible,.statecard:focus-visible,tr.row:focus-visible,.card:focus-visible{
  outline:2px solid var(--dem);outline-offset:2px;border-radius:6px}
.seg button[aria-selected=true]{background:var(--ink);color:#fff}
.sr-only{position:absolute;width:1px;height:1px;padding:0;margin:-1px;overflow:hidden;clip:rect(0,0,0,0);white-space:nowrap;border:0}

/* ===== Hat-in-Ring Radar scope (design graft — self-contained navy command-center; reuses the page's --dem/--rep/--lib/--ind/--t* tokens) ===== */
.hir-radar{--navy:#0c1730;--navy-2:#0a1226;--sky:#bacaef;margin:14px 0 2px;padding:16px 18px 14px;background:radial-gradient(120% 140% at 50% -10%,#16264a 0%,var(--navy) 55%,var(--navy-2) 100%);border:1px solid #1e2f54;border-radius:16px;color:#e8eefc;box-shadow:0 10px 34px rgba(6,12,28,.45),inset 0 1px 0 rgba(255,255,255,.04)}
.hir-radar-head{display:flex;align-items:flex-end;gap:14px;flex-wrap:wrap;margin-bottom:6px}
.hir-script{font-family:'Pinyon Script',cursive;font-size:30px;line-height:.9;color:#fff;text-shadow:0 1px 10px rgba(120,160,255,.35)}
.hir-sub{font-family:'Space Grotesk',system-ui,sans-serif;font-size:11.5px;letter-spacing:.04em;color:var(--sky);opacity:.82}
.hir-legend{display:flex;gap:12px;margin-left:auto;font-family:'Space Grotesk',system-ui,sans-serif;font-size:11px;color:var(--sky)}
.hir-legend .lm{display:inline-flex;align-items:center;gap:5px}
.hir-legend .lm i{width:8px;height:8px;border-radius:50%;box-shadow:0 0 6px currentColor}
.hir-ctrls{display:flex;gap:6px;align-items:center}
.hir-radar button{font:inherit;font-size:13px;cursor:pointer;background:rgba(255,255,255,.06);color:#cfe0ff;border:1px solid #2a3c64;border-radius:8px;padding:5px 9px;line-height:1}
.hir-radar button.on,.hir-radar button:hover{background:rgba(120,160,255,.18);border-color:#3a5286}
.hir-scope{position:relative;width:100%;max-width:560px;margin:6px auto 0;aspect-ratio:1/1}
.hir-radar[data-dir="briefing"] .hir-scope{max-width:340px}
.hir-scope canvas{position:absolute;inset:0;width:100%;height:100%}
.hir-blips{position:absolute;inset:0}
.hir-blip{position:absolute;width:var(--bs,13px);height:var(--bs,13px);margin:calc(var(--bs,13px)/-2) 0 0 calc(var(--bs,13px)/-2);border-radius:50%;background:var(--bc,#8aa);box-shadow:0 0 8px var(--bc,#8aa),inset 0 0 0 1px rgba(255,255,255,.28);cursor:pointer;transition:transform .12s ease}
.hir-blip:hover,.hir-blip:focus-visible{transform:scale(1.28);z-index:5;outline:none}
.hir-blip .star{position:absolute;inset:0;display:flex;align-items:center;justify-content:center;color:#ffe07a;font-size:calc(var(--bs,13px)*.72);text-shadow:0 0 4px rgba(0,0,0,.6)}
.hir-blip.ping{animation:hirping .9s ease-out}
@keyframes hirping{0%{box-shadow:0 0 0 0 var(--bc),0 0 8px var(--bc)}100%{box-shadow:0 0 0 15px transparent,0 0 8px var(--bc)}}
.hir-blip .hir-tip{position:absolute;left:50%;bottom:150%;transform:translateX(-50%);white-space:nowrap;background:#0a1226;border:1px solid #2a3c64;border-radius:8px;padding:6px 9px;font-family:'Space Grotesk',system-ui,sans-serif;font-size:11px;color:#e8eefc;opacity:0;pointer-events:none;transition:opacity .12s;box-shadow:0 8px 22px rgba(0,0,0,.5);z-index:6}
.hir-blip:hover .hir-tip,.hir-blip:focus-visible .hir-tip{opacity:1}
.hir-tip .bn{font-weight:600}.hir-tip .br{color:var(--sky);font-size:10px}
.hir-foot{display:flex;gap:16px;flex-wrap:wrap;justify-content:center;margin-top:10px;font-family:'Space Grotesk',system-ui,sans-serif;font-size:10.5px;color:var(--sky);opacity:.78}
.hir-foot b{color:#fff}
.hir-tweaks{position:fixed;right:16px;bottom:16px;z-index:60;width:236px;background:#0a1226;border:1px solid #2a3c64;border-radius:12px;padding:14px 14px 8px;color:#e8eefc;font-family:'Space Grotesk',system-ui,sans-serif;font-size:12px;box-shadow:0 16px 40px rgba(0,0,0,.5)}
.hir-tweaks[hidden]{display:none}
.hir-tweaks h4{margin:0 0 8px;font-family:'Pinyon Script',cursive;font-size:21px;font-weight:400;color:#fff}
.hir-tweaks .row{display:flex;align-items:center;justify-content:space-between;gap:8px;margin:9px 0}
.hir-tweaks input[type=range]{width:112px}
@media (prefers-reduced-motion: reduce){.hir-blip,.hir-blip.ping{animation:none;transition:none}}
@media (max-width:640px){.hir-script{font-size:24px}.hir-legend{width:100%;margin:4px 0 0}.hir-tweaks{left:12px;right:12px;width:auto}}
</style>
</head>
<body>
<div class="wrap">
  <header class="top">
    <div class="brand">
      <h1>🧭 Hat-in-Ring Radar</h1>
      <div class="sub">2028 presidential signal tracker</div>
    </div>
    <div class="asof">
      As of <b id="asof">{{ as_of }}</b><br>
      <span id="counter"></span><br>
      <span class="buildstamp" title="Last automated rebuild (UTC in CI)">auto-built {{ generated_at_human }}</span>
    </div>
  </header>

  <section class="crawl-summary" id="crawl-summary">
    <p>The <b>2028 Hat-in-Ring Radar</b> tracks {{ crawl_count }} potential 2028 U.S. presidential candidates on two axes — <b>status tier</b> (the furthest verifiable step taken) and <b>momentum</b> (weighted activity + recency, 0–100) — rebuilt daily from FEC filings and news. Status is not the same as support. Live board: <a href="{{ canonical_url }}">systembydave.com/hat-in-ring</a>.</p>
    <noscript>
      <table><thead><tr><th>#</th><th>Candidate</th><th>Lane</th><th>Status</th><th>Momentum</th></tr></thead>
      <tbody>{{ crawl_rows }}</tbody></table>
    </noscript>
  </section>

  <div class="stats" id="stats"></div>
  <div class="nudge hide" id="nudge"></div>

  <!-- ===== Hat-in-Ring Radar scope (design graft) ===== -->
  <section class="hir-radar" id="scopeHost" data-dir="command" aria-label="Candidate radar scope">
    <div class="hir-radar-head">
      <div>
        <div class="hir-script">Live Scope</div>
        <div class="hir-sub">who's stepping into the ring — and how hot</div>
      </div>
      <div class="hir-legend" id="hirScopeLegend"></div>
      <div class="hir-ctrls">
        <button type="button" class="hir-dirbtn on" data-hirdir="command" title="Command view" aria-label="Command view">◎</button>
        <button type="button" class="hir-dirbtn" data-hirdir="briefing" title="Briefing (compact)" aria-label="Briefing view">▭</button>
        <button type="button" id="hirTweakBtn" title="Radar tweaks" aria-label="Radar tweaks">⚙</button>
      </div>
    </div>
    <div class="hir-scope" id="hirScope">
      <canvas id="hirScopeCanvas"></canvas>
      <div class="hir-blips" id="hirBlips"></div>
    </div>
    <div class="hir-foot">
      <span><b>Centre</b> = high momentum</span>
      <span><b>Angle</b> = party lane</span>
      <span><b>Dot size</b> = confidence · <b>★</b> = poll leader</span>
    </div>
  </section>
  <div class="hir-tweaks" id="hirTweaks" hidden>
    <h4>Radar Tweaks</h4>
    <div class="row"><label for="hirRadarOn">Radar sweep</label><button type="button" id="hirRadarOn" class="on">On</button></div>
    <div class="row"><label for="hirSpeed">Sweep speed</label><input type="range" id="hirSpeed" min="2" max="12" step="1" value="6"></div>
    <div class="row"><label for="hirDirSel">Layout</label><button type="button" id="hirDirSel">Command</button></div>
  </div>

  <div class="toolbar">
    <div class="grow"><input type="search" id="q" list="qhints" placeholder="Search name or role…" autocomplete="off"><datalist id="qhints"></datalist></div>
    <div class="chips" id="partyChips"></div>
    <select id="bucketSel" title="Seriousness bucket">
      <option value="">All buckets</option>
      <option value="formal">Formal (declared/exploratory)</option>
      <option value="considering">Actively considering</option>
      <option value="soft">Soft / positioning</option>
      <option value="edge">Edge / conditional</option>
      <option value="out">Ruled out / inactive</option>
    </select>
    <select id="confSel" title="Min confidence">
      <option value="">Any confidence</option>
      <option value="3">Verified only (High+)</option>
      <option value="2">Medium+</option>
      <option value="1">Hide noise</option>
    </select>
    <div class="seg" id="viewSeg" role="tablist" aria-label="Dashboard views">
      <button data-view="board" role="tab" aria-selected="true" class="on">Leaderboard <span class="segbadge alt" id="moversCount" title="movers"></span></button>
      <button data-view="cards" role="tab" aria-selected="false">Cards</button>
      <button data-view="traj" role="tab" aria-selected="false">Trajectory</button>
      <button data-view="early" role="tab" aria-selected="false">Early&nbsp;States</button>
      <button data-view="compare" role="tab" aria-selected="false">Compare <span class="segbadge alt" id="compareCount"></span></button>
      <button data-view="review" role="tab" aria-selected="false">Review <span class="segbadge" id="reviewCount"></span></button>
    </div>
    <button class="btn" id="addBtn">+ Add</button>
    <button class="btn" id="exportBtn" title="Download data as JSON">Export</button>
  </div>

  <div class="quickbar" id="quickbar"></div>

  <div class="layout">
    <div>
      <div id="stateFilter"></div>
      <div class="panel" id="boardPanel">
        <h2>Seriousness leaderboard <span class="meta" id="boardMeta"></span></h2>
        <table id="board"><thead><tr>
          <th class="rank">#</th>
          <th data-sort="name">Candidate</th>
          <th data-sort="party">Lane</th>
          <th data-sort="tierRank">Status</th>
          <th data-sort="score" class="sorted">Momentum</th>
          <th data-sort="slope30" class="trendCell">Trend</th>
          <th data-sort="delta">7‑day</th>
          <th data-sort="confRank">Conf.</th>
        </tr></thead><tbody id="boardBody"></tbody></table>
      </div>
      <div class="panel hide" id="cardsPanel">
        <h2>Candidate cards <span class="meta" id="cardsMeta"></span></h2>
        <div class="cards" id="cards"></div>
      </div>
      <div class="panel hide" id="reviewPanel">
        <h2>Review queue <span class="meta" id="reviewMeta"></span></h2>
        <div class="reviewNote">Discoveries and ambiguous denials the pipeline routed here instead of auto-applying. <b>Confirm</b> applies the signal to the board; <b>Dismiss</b> drops it. Decisions save in this browser — click <b>Export decisions</b> and commit <code>data/review_decisions.json</code> to apply them in the next rebuild.</div>
        <div id="reviewList"></div>
        <div class="reviewActions">
          <button class="btn primary" id="exportDecisionsBtn">Export decisions</button>
          <span class="meta" id="decisionsMeta"></span>
        </div>
      </div>

      <div class="panel hide" id="trajPanel">
        <h2>Momentum trajectory <span class="meta" id="trajMeta"></span></h2>
        <div class="trajCtl">Sort by
          <div class="seg" id="trajSeg" role="group" aria-label="Trajectory window">
            <button data-win="7" class="on">7‑day</button><button data-win="30">30‑day</button>
          </div> momentum change · sparkline shows recent momentum.</div>
        <table id="trajTbl"><thead><tr><th class="rank">#</th><th>Candidate</th><th>Trend</th><th>Momentum</th><th>Δ7d</th><th>Δ30d</th></tr></thead><tbody id="trajBody"></tbody></table>
      </div>

      <div class="panel hide" id="earlyPanel">
        <h2>Early‑state activity <span class="meta">Iowa · New Hampshire · South Carolina · Nevada</span></h2>
        <div class="bigboard" id="bigboard"></div>
      </div>

      <div class="panel hide" id="comparePanel">
        <h2>Head‑to‑head compare <span class="meta" id="cmpMeta"></span></h2>
        <div id="cmpBody"></div>
      </div>
    </div>

    <aside>
      <div class="panel" style="margin-bottom:16px">
        <h2>Today's briefing <span class="meta" id="briefMeta">what moved</span></h2>
        <div class="briefing" id="briefing"></div>
      </div>
      <div class="panel" style="margin-bottom:16px">
        <h2>Today's movement <span class="meta">latest signals</span></h2>
        <div class="feed" id="feed"></div>
      </div>
      <div class="panel">
        <h2>Status tiers</h2>
        <div class="legend" id="legend"></div>
      </div>
    </aside>
  </div>

  <footer id="foot"></footer>
</div>

<!-- drawer -->
<div class="scrim" id="scrim"></div>
<div class="drawer" id="drawer"><div id="drawerInner"></div></div>
<div class="toast" id="toast" role="status" aria-live="polite"></div>
<div class="cmptray hide" id="cmptray" aria-live="polite"></div>

<script>
const GENERATED_AT = {{ generated_at }};   // build timestamp (drives recency maths)
/* ============================================================
   SCORING ENGINE  (refined model — see footer / spec notes)
   Two orthogonal axes:
     • STATUS TIER = furthest verifiable step taken (categorical)
     • MOMENTUM SCORE 0–100 = weighted sum of activity signals
   Status takes the single highest legal/declarative signal (no
   stacking). Momentum adds behavioural signals + a continuous
   recency term so scores cool over time instead of a single cliff.
   ============================================================ */
const TODAY = new Date(GENERATED_AT);

const WEIGHTS = {
  declared:        {w:40, label:'Formal FEC candidacy / launch'},
  exploratory:     {w:30, label:'Exploratory committee'},
  consideringQuote:{w:20, label:'Direct "considering" quote'},
  softConsidering: {w:12, label:'Soft / "not ruling out" quote'},
  earlyState:      {w:10, label:'Early-state travel (IA/NH/SC/NV)'},
  donors:          {w:10, label:'Donor meetings / PAC activity'},
  staffing:        {w:10, label:'Campaign staffing / consultants'},
  mediaBlitz:      {w:5,  label:'National media blitz'},
  endorsedOther:   {w:-20,label:'Endorsed another likely candidate'},
  ruledOut:        {w:-40,label:'Explicitly ruled out'},
  barred:          {w:-100,label:'Constitutionally ineligible'}
};
const TIERS = {
  5:{label:'Declared',      c:'var(--t5)'},
  4:{label:'Exploratory',   c:'var(--t4)'},
  3:{label:'Considering',   c:'var(--t3)'},
  2:{label:'Positioning',   c:'var(--t2)'},
  1:{label:'Floated',       c:'var(--t1)'},
  0:{label:'Inactive',      c:'var(--t0)'}
};
const CONF = {'Very high':4,'High':3,'Medium':2,'Low':1,'Noise':0};
const CONFC = {'Very high':'var(--good)','High':'#3a8f55','Medium':'var(--lib)','Low':'#b08a2a','Noise':'#b0b0b0'};
const PARTYC = {Democrat:'var(--dem)',Republican:'var(--rep)',Libertarian:'var(--lib)',Independent:'var(--ind)'};

function daysSince(d){return Math.floor((TODAY-new Date(d))/86400000);}
function recencyTerm(d){const n=daysSince(d); if(n<=30)return {w:5,label:`Recency boost (signal ${n}d ago)`}; if(n>90)return {w:-10,label:`Stale (no signal ${n}d)`}; return null;}

function deriveStatus(keys){
  if(keys.includes('barred')) return {tier:0,label:'Ineligible (22nd Amdt)'};
  if(keys.includes('ruledOut')) return {tier:0,label:'Ruled out'};
  if(keys.includes('declared')) return {tier:5,label:'Declared'};
  if(keys.includes('exploratory')) return {tier:4,label:'Exploratory'};
  if(keys.includes('consideringQuote')) return {tier:3,label:'Actively considering'};
  const behav = keys.some(k=>['earlyState','donors','staffing','mediaBlitz'].includes(k));
  if(behav) return {tier:2,label:'Positioning'};
  if(keys.includes('softConsidering')) return {tier:1,label:'Floated / not ruling out'};
  return {tier:0,label:'Inactive'};
}
function breakdown(c){
  const items=[];
  // status axis: single highest declarative signal only
  const order=['declared','exploratory','consideringQuote','softConsidering'];
  const top=order.find(k=>c.keys.includes(k));
  if(top) items.push({label:WEIGHTS[top].label,w:WEIGHTS[top].w});
  // behavioural: all of them stack
  ['earlyState','donors','staffing','mediaBlitz'].forEach(k=>{ if(c.keys.includes(k)) items.push({label:WEIGHTS[k].label,w:WEIGHTS[k].w}); });
  // recency
  const r=recencyTerm(c.lastSignal); if(r) items.push(r);
  // penalties
  ['endorsedOther','ruledOut','barred'].forEach(k=>{ if(c.keys.includes(k)) items.push({label:WEIGHTS[k].label,w:WEIGHTS[k].w}); });
  return items;
}
function score(c){
  let s=breakdown(c).reduce((a,b)=>a+b.w,0);
  return Math.max(0,Math.min(100,s));
}

/* ============================================================
   SEED DATA  — compiled from Dave's vetted 2028 list (Jun 12 2026)
   Sources: AP, Reuters, Time, WaPo, ABC, C-SPAN, FEC, Wikipedia.
   keys[] drive both status and score; edit/add persists locally.
   ============================================================ */
const SEED = {{ seed_json }};
/* REVIEW QUEUE — discoveries + ambiguous denials the pipeline routed to a human
   instead of auto-applying. Triage here; Export decisions to apply in the rebuild. */
const REVIEW = {{ review_json }};
/* BRIEFING — the pipeline-baked daily "what moved" summary (top movers, new
   filers, status changes, review backlog). Drives the Today's Briefing panel. */
const BRIEFING = {{ briefing_json }};
const EARLY_NAMES={IA:'Iowa',NH:'New Hampshire',SC:'South Carolina',NV:'Nevada'};

/* ---------- persistence ---------- */
const LS='hatInRing_v1';
function load(){
  let saved=null;
  try{ const raw=localStorage.getItem(LS); saved=raw?JSON.parse(raw):null; }catch(e){}
  const fresh=JSON.parse(JSON.stringify(SEED));
  if(!saved||!saved.records||!saved.records.length) return {records:fresh,prefs:{},ver:GENERATED_AT};
  if(saved.ver===GENERATED_AT) return saved;            // same pipeline build → keep user edits
  const manual=saved.records.filter(r=>String(r.id).startsWith('c')); // preserve manual-only adds
  return {records:fresh.concat(manual),prefs:saved.prefs||{},ver:GENERATED_AT};
}
function save(){ try{localStorage.setItem(LS,JSON.stringify(DB));}catch(e){} }
let DB=load();
// Merge saved prefs *over* the defaults. NB: an empty saved object ({}) is truthy,
// so `DB.prefs || {defaults}` would skip the defaults on first visit and leave
// PREFS.party undefined → filtered() crashes the whole board. Object.assign guarantees
// every key (party array, sort, dir…) always exists.
let PREFS=Object.assign({view:'board',party:[],bucket:'',conf:'',q:'',sort:'score',dir:'desc',quick:'',earlyState:'',trajWin:7},DB.prefs||{});

/* ---------- review-queue decisions (local until exported) ---------- */
const RLS='hatInRing_reviewDecisions_v1';
function loadDecisions(){ try{return JSON.parse(localStorage.getItem(RLS))||{};}catch(e){return {};} }
let DECISIONS=loadDecisions();                 // { rid: 'confirm' | 'dismiss' }
function saveDecisions(){ try{localStorage.setItem(RLS,JSON.stringify(DECISIONS));}catch(e){} }
function decide(rid,action){ DECISIONS[rid]=action; saveDecisions(); renderReview(); toast(action==='confirm'?'Confirmed — export to apply':'Dismissed'); }

/* ---------- head-to-head compare (deep-linkable via #compare=id1,id2) ---------- */
const CMP_MAX=3;
function parseCompareHash(){try{const h=(typeof location!=='undefined'&&location.hash)?location.hash:'';const m=/[#&]compare=([^&]+)/.exec(h);return m?decodeURIComponent(m[1]).split(',').map(s=>s.trim()).filter(Boolean).slice(0,CMP_MAX):[];}catch(e){return [];}}
let COMPARE=parseCompareHash();
function writeCompareHash(){const h=COMPARE.length?('#compare='+COMPARE.join(',')):'';try{history.replaceState(null,'',location.pathname+location.search+h);}catch(e){}}
function toggleCompare(id){const i=COMPARE.indexOf(id);if(i>=0)COMPARE.splice(i,1);else{if(COMPARE.length>=CMP_MAX){toast('Compare up to '+CMP_MAX+' at once');return;}COMPARE.push(id);}writeCompareHash();renderCmpTray();renderCards();if(PREFS.view==='compare')renderCompare();}
function clearCompare(){COMPARE=[];writeCompareHash();renderCmpTray();renderCards();if(PREFS.view==='compare')renderCompare();}

/* ---------- anticipatory UX state + microinteraction helpers (device-local, privacy-aware) ---------- */
const UXLS='hatInRing_ux_v1';
function loadUX(){try{return Object.assign({lastSeenVer:'',recentSearches:[],dismissedNudges:{}},JSON.parse(localStorage.getItem(UXLS))||{});}catch(e){return {lastSeenVer:'',recentSearches:[],dismissedNudges:{}};}}
let UX=loadUX();
function saveUX(){try{localStorage.setItem(UXLS,JSON.stringify(UX));}catch(e){}}
const FIRST_VISIT=!(DB.prefs&&DB.prefs.view);
if(FIRST_VISIT&&(window.innerWidth||1200)<700)PREFS.view='cards';      // mobile-first default view

// "what's worth your attention" predicates
function isFresh(x){return daysSince(x.lastSignal)<=7;}                 // a fresh signal this week
function isMover(x){return x.delta!==0||isFresh(x);}
// "My adds" = user-created (`c<timestamp>`) or review-confirmed (`rev-…`) records.
// NB: `c` must be followed by a digit so real candidate ids like "cruz" don't match.
const QUICK={movers:{label:'Movers',f:isMover},declared:{label:'Declared',f:x=>x.tier===5},myadds:{label:'My adds',f:x=>/^(c\d|rev-)/.test(String(x.id))}};

// microinteraction feedback helpers (all motion <200ms, purposeful)
let _toastT=null;
function toast(msg,isErr){const t=$('#toast');if(!t)return;t.textContent=msg;t.className='toast on'+(isErr?' err':'');clearTimeout(_toastT);_toastT=setTimeout(function(){t.className='toast'+(isErr?' err':'');},1600);}
function flashRow(id){const el=[].slice.call(document.querySelectorAll('[data-id]')).find(function(e){return e.dataset.id===id;});if(el){el.classList.remove('flash');void el.offsetWidth;el.classList.add('flash');}}
function shake(el){if(!el)return;el.classList.add('shake');setTimeout(function(){el.classList.remove('shake');},220);}
function pop(el){if(!el)return;el.classList.add('pop');setTimeout(function(){el.classList.remove('pop');},170);}
function fadePanel(){const map={board:'#boardPanel',cards:'#cardsPanel',review:'#reviewPanel'};const el=$(map[PREFS.view]);if(el){el.classList.remove('fadein');void el.offsetWidth;el.classList.add('fadein');}}

/* ---------- derived ---------- */
function enrich(c){
  const st=deriveStatus(c.keys); const sc=score(c);
  return Object.assign({},c,{tier:st.tier,statusLabel:st.label,tierRank:st.tier,score:sc,
    confRank:CONF[c.conf]??0});
}
function records(){ return DB.records.map(enrich); }

/* ---------- filtering ---------- */
function filtered(){
  let r=records();
  if(PREFS.party.length) r=r.filter(x=>PREFS.party.includes(x.party));
  if(PREFS.bucket) r=r.filter(x=>x.bucket===PREFS.bucket);
  if(PREFS.conf) r=r.filter(x=>x.confRank>=+PREFS.conf);
  if(PREFS.q){const q=PREFS.q.toLowerCase(); r=r.filter(x=>(x.name+' '+x.role).toLowerCase().includes(q));}
  if(PREFS.quick&&QUICK[PREFS.quick]) r=r.filter(QUICK[PREFS.quick].f);
  if(PREFS.earlyState) r=r.filter(x=>x.early_states&&x.early_states[PREFS.earlyState]);
  const dir=PREFS.dir==='asc'?1:-1;
  r.sort((a,b)=>{
    let va=a[PREFS.sort],vb=b[PREFS.sort];
    if(typeof va==='string'){return va.localeCompare(vb)*dir;}
    if(va===vb){return b.score-a.score;}
    return (va-vb)*dir;
  });
  return r;
}

/* ---------- render ---------- */
const $=s=>document.querySelector(s);
// Escape every untrusted record field before it goes into innerHTML. Headlines,
// quotes and names flow in from live news/FEC ingest and the add/edit form, so a
// payload like <img src=x onerror=...> would otherwise execute on render.
function esc(s){return String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}
function partyColor(p){return PARTYC[p]||'var(--other)';}

function renderStats(){
  const r=records();
  const by=t=>r.filter(x=>x.tier===t).length;
  const cards=[
    {n:r.filter(x=>x.bucket==='formal').length,l:'Formal',c:'var(--t4)'},
    {n:r.filter(x=>x.tier===3).length,l:'Considering',c:'var(--t3)'},
    {n:r.filter(x=>x.tier===2).length,l:'Positioning',c:'var(--t2)'},
    {n:r.filter(x=>x.tier===1).length,l:'Floated',c:'var(--t1)'},
    {n:r.filter(x=>x.bucket==='out').length,l:'Ruled out',c:'var(--t0)'},
    {n:r.length,l:'Tracked',c:'var(--ink)'}
  ];
  $('#stats').innerHTML=cards.map(c=>`<div class="stat"><div class="n">${c.n}</div><div class="l"><span class="dot" style="background:${c.c}"></span>${c.l}</div></div>`).join('');
  $('#counter').textContent=`${r.length} names · ${r.filter(x=>daysSince(x.lastSignal)<=14).length} moved this fortnight`;
  $('#moversCount').textContent=r.filter(isMover).length||'';
}

function tierBadge(x){return `<span class="tier" style="background:${TIERS[x.tier].c}">${x.statusLabel}</span>`;}
function deltaHTML(d){const cls=d>0?'up':d<0?'dn':'fl';const s=d>0?'▲ +'+d:d<0?'▼ '+d:'– 0';return `<span class="delta ${cls}">${s}</span>`;}
function confHTML(c){return `<span class="conf"><span class="dotc" style="background:${CONFC[c]}"></span>${esc(c)}</span>`;}

function renderBoard(){
  const r=filtered();
  $('#boardMeta').textContent=r.length+' shown';
  $('#boardBody').innerHTML=r.map((x,i)=>`
    <tr class="row" data-id="${x.id}">
      <td class="rank">${i+1}</td>
      <td><span class="pbar" style="background:${partyColor(x.party)}"></span>${x.img?`<img class="avatarSm" src="${esc(x.img)}" alt="" loading="lazy">`:''}
          <span class="nm">${esc(x.name)}${isFresh(x)?`<span class="newdot" title="New signal this week"></span>`:''}${x.pollLead?`<span class="pollLead">★</span>`:''}<span class="role">${esc(x.role)}</span></span></td>
      <td>${esc(x.party)}</td>
      <td>${tierBadge(x)}</td>
      <td class="scoreCell">${x.score}<span class="track"><i style="width:${x.score}%;background:${TIERS[x.tier].c}"></i></span></td>
      <td class="trendCell">${sparkline(x.series)}${slopeHTML(x.slope30)}</td>
      <td>${deltaHTML(x.delta)}</td>
      <td>${confHTML(x.conf)}</td>
    </tr>`).join('');
  document.querySelectorAll('#board th[data-sort]').forEach(th=>{
    th.classList.toggle('sorted',th.dataset.sort===PREFS.sort);
    th.classList.toggle('asc',PREFS.dir==='asc');
  });
}
function renderCards(){
  const r=filtered();
  $('#cardsMeta').textContent=r.length+' shown';
  $('#cards').innerHTML=r.map(x=>`
    <div class="card" data-id="${x.id}">
      <span class="accent" style="background:${partyColor(x.party)}"></span>
      <div class="ch">${x.img?`<img class="avatar" src="${esc(x.img)}" alt="" loading="lazy">`:''}<div style="flex:1;min-width:0"><div class="nm2">${esc(x.name)}${isFresh(x)?`<span class="newdot" title="New signal this week"></span>`:''}${x.pollLead?`<span class="pollLead">★</span>`:''}</div><div class="role2">${esc(x.role)}</div></div>${tierBadge(x)}</div>
      ${stateFlags(x)}
      <div class="sig">${esc(x.headline)}</div>
      <div class="foot"><div class="scoreBig" style="color:${TIERS[x.tier].c}">${x.score}</div>
        <div style="display:flex;align-items:center;gap:8px">${sparkline(x.series)}<div style="text-align:right">${deltaHTML(x.delta)}<br>${confHTML(x.conf)}</div></div></div>
      <label style="display:inline-flex;gap:5px;align-items:center;font-size:11px;color:var(--muted);margin:9px 0 0 6px;cursor:pointer"><input type="checkbox" class="cmp-pick" data-cmp="${x.id}" ${COMPARE.includes(x.id)?'checked':''} aria-label="Add ${esc(x.name)} to head-to-head compare"> compare</label>
    </div>`).join('');
}
function renderFeed(){
  const r=records().filter(x=>x.bucket!=='out'||x.delta!==0)
    .sort((a,b)=>new Date(b.lastSignal)-new Date(a.lastSignal)).slice(0,9);
  $('#feed').innerHTML=r.map(x=>{
    const d=new Date(x.lastSignal).toLocaleDateString('en-US',{month:'short',day:'numeric'});
    return `<div class="it" data-id="${x.id}" style="cursor:pointer">
      <div class="d">${d} · ${esc(x.party)} ${x.delta?'· '+deltaHTML(x.delta):''}</div>
      <div class="h"><b>${esc(x.name)}</b> — ${esc(x.headline)}</div></div>`;
  }).join('');
}
function renderLegend(){
  $('#legend').innerHTML=Object.entries(TIERS).sort((a,b)=>b[0]-a[0]).map(([t,o])=>
    `<div class="li"><span class="sw"><span class="tier" style="background:${o.c};padding:2px 6px">${t}</span></span><span>${({5:'Declared — FEC Form 2 / launch',4:'Exploratory committee / testing-the-waters org',3:'Direct "considering" quote',2:'Positioning — travel, donors, staff, media',1:'Floated / "not ruling out"',0:'Ruled out / ineligible / inactive'})[t]}</span></div>`
  ).join('')+`<div class="note">★ = leads primary polling despite lower status tier (status ≠ support).</div>`;
}
const _KINDS={discovery:['Discovery','disc'],denial:['Denial · confirm','den'],fec:['FEC filer','fec']};
function kindBadge(k){const [l,c]=_KINDS[k]||['Review','disc'];return `<span class="kind ${c}">${l}</span>`;}
function reviewPending(){return REVIEW.filter(x=>!DECISIONS[x.rid]);}
function safeUrl(u){return /^https?:\/\//i.test(u||'')?u:'';}
function renderReview(){
  const items=reviewPending();
  $('#reviewCount').textContent=items.length||'';
  const decided=Object.keys(DECISIONS).length;
  $('#reviewMeta').textContent=`${items.length} pending · ${decided} decided`;
  $('#decisionsMeta').textContent=decided?`${decided} decision${decided>1?'s':''} ready — export & commit`:'no decisions yet';
  $('#reviewList').innerHTML=items.map(x=>{
    const url=safeUrl(x.url);
    return `<div class="reviewItem" data-rid="${esc(x.rid)}">
      <div class="ri-head"><b>${esc(x.name)}</b> ${kindBadge(x.kind)}<span class="ri-src">${esc(x.source||'')}${x.date?' · '+esc(x.date):''}</span></div>
      <div class="ri-headline">${esc(x.headline||'')}</div>
      <div class="ri-keys">${(Array.isArray(x.keys)?x.keys:[]).map(k=>`<span class="kchip">${esc(k)}</span>`).join('')}</div>
      <div class="ri-act"><button class="btn primary" data-confirm="${esc(x.rid)}">Confirm</button><button class="btn" data-dismiss="${esc(x.rid)}">Dismiss</button>${url?`<a class="ri-link" href="${esc(url)}" target="_blank" rel="noopener noreferrer">source ↗</a>`:''}</div>
    </div>`;
  }).join('')||`<div class="reviewEmpty">Nothing in the review queue. 🎉</div>`;
}
function renderQuick(){
  const all=records();
  $('#quickbar').innerHTML=Object.keys(QUICK).map(function(k){
    const n=all.filter(QUICK[k].f).length;
    return `<span class="qchip${PREFS.quick===k?' on':''}" data-quick="${k}" role="button" tabindex="0">${QUICK[k].label}<span class="qn">${n}</span></span>`;
  }).join('');
}
function renderHints(){const dl=$('#qhints'); if(dl) dl.innerHTML=(UX.recentSearches||[]).map(s=>`<option value="${esc(s)}">`).join('');}
function renderNudge(){
  const n=$('#nudge'); if(!n) return;
  if(!FIRST_VISIT && UX.lastSeenVer && UX.lastSeenVer!==GENERATED_AT && !UX.dismissedNudges[GENERATED_AT]){
    const m=records().filter(isMover).length;
    n.innerHTML=`Updated since your last visit — <b>${m}</b> mover${m===1?'':'s'} this week.<button class="btn" id="showMovers" style="margin-left:auto">Show movers</button><span class="x" id="nudgeX" title="Dismiss">×</span>`;
    n.classList.remove('hide');
  } else { n.classList.add('hide'); }
}
/* ---------- v2 helpers: sparkline, slope, flags, money ---------- */
function sparkline(series){
  const pts=(series||[]).filter(p=>typeof p.s==='number');
  const W=64,H=18,pad=2;
  if(pts.length<2) return `<svg class="spark" width="${W}" height="${H}" aria-hidden="true"><line x1="2" y1="${H/2}" x2="${W-2}" y2="${H/2}" stroke="#d6d3ca" stroke-width="1.5"/></svg>`;
  const X=i=>pad+(i/(pts.length-1))*(W-2*pad), Y=v=>H-pad-(Math.max(0,Math.min(100,v))/100)*(H-2*pad);
  const d=pts.map((p,i)=>`${i?'L':'M'}${X(i).toFixed(1)} ${Y(p.s).toFixed(1)}`).join(' ');
  const first=pts[0].s,last=pts[pts.length-1].s;
  const col=last>first?'var(--good)':last<first?'var(--bad)':'var(--muted)';
  return `<svg class="spark" width="${W}" height="${H}" viewBox="0 0 ${W} ${H}" role="img" aria-label="Momentum trend over ${pts.length} readings, now ${last}"><path d="${d}" fill="none" stroke="${col}" stroke-width="1.5"/><circle cx="${X(pts.length-1).toFixed(1)}" cy="${Y(last).toFixed(1)}" r="1.9" fill="${col}"/></svg>`;
}
function slopeHTML(n){n=n||0;const cls=n>0?'up':n<0?'dn':'fl';return `<span class="slopen ${cls}">${n>0?'+'+n:''+n}</span>`;}
function stateFlags(x){
  const f=[];
  if((x.slope7||0)>0)f.push('<span class="flag hot" title="Momentum rising vs last week">▲ Heating</span>');
  else if((x.slope7||0)<0)f.push('<span class="flag cool" title="Momentum falling vs last week">▼ Cooling</span>');
  if(daysSince(x.lastSignal)>90)f.push('<span class="flag stale" title="No signal in 90+ days">Stale</span>');
  if((x.confRank||0)>=3)f.push('<span class="flag verified" title="High-confidence sourcing">Verified</span>');
  return f.length?`<div class="flags">${f.join('')}</div>`:'';
}
function fmtMoney(v){if(v==null||isNaN(v))return '—';const a=Math.abs(v);if(a>=1e9)return '$'+(v/1e9).toFixed(1)+'B';if(a>=1e6)return '$'+(v/1e6).toFixed(1)+'M';if(a>=1e3)return '$'+Math.round(v/1e3)+'K';return '$'+v;}
function tierTimeline(series){
  const pts=(series||[]).filter(p=>p.t!=null);
  const ch=[];let prev=null;
  pts.forEach(p=>{if(p.t!==prev){ch.push(p);prev=p.t;}});
  if(ch.length<2)return '';
  return '<div style="margin-top:8px">'+ch.map(p=>`<div class="cmp"><span>${new Date(p.d).toLocaleDateString('en-US',{month:'short',day:'numeric',year:'numeric'})}</span><span class="v">${(TIERS[p.t]||{}).label||p.t}</span></div>`).join('')+'</div>';
}

/* ---------- v2 renderers ---------- */
function renderBriefing(){
  const el=$('#briefing'); if(!el) return;
  const b=BRIEFING||{}; const sec=[];
  const mv=(b.movers||[]).slice(0,5);
  if(mv.length) sec.push('<div class="b-sec">Top movers</div>'+mv.map(m=>`<div class="b-item" data-id="${m.id}"><span>${esc(m.name)}</span><span class="b-d ${m.delta>0?'up':m.delta<0?'dn':'fl'}">${m.delta>0?'▲ +'+m.delta:m.delta<0?'▼ '+m.delta:'– 0'} · ${m.score}</span></div>`).join(''));
  const nf=(b.new_filers||[]);
  if(nf.length) sec.push('<div class="b-sec">Formal / new filers</div>'+nf.slice(0,5).map(m=>`<div class="b-item" data-id="${m.id}"><span>${esc(m.name)}</span><span class="conf">${esc(m.statusLabel)}</span></div>`).join(''));
  const tr=(b.transitions||[]);
  if(tr.length) sec.push('<div class="b-sec">Status changes (7d)</div>'+tr.slice(0,5).map(t=>`<div class="b-item"><span>${esc(t.name)}</span><span class="conf">${(TIERS[t.from]||{}).label||t.from} → ${(TIERS[t.to]||{}).label||t.to}</span></div>`).join(''));
  el.innerHTML=(sec.join('')||'<div class="b-empty">No notable movement today.</div>')+`<div class="b-share">${b.review_count?b.review_count+' item'+(b.review_count>1?'s':'')+' awaiting review · ':''}<a href="share.html" target="_blank" rel="noopener">Open share card ↗</a></div>`;
  $('#briefMeta').textContent=b.date||'what moved';
}
function renderTrajectory(){
  const win=PREFS.trajWin===30?30:7, key=win===30?'slope30':'slope7';
  const r=records().slice().sort((a,b)=>(b[key]-a[key])||(b.score-a.score));
  $('#trajMeta').textContent=r.length+' tracked';
  document.querySelectorAll('#trajSeg button').forEach(b=>b.classList.toggle('on',+b.dataset.win===win));
  $('#trajBody').innerHTML=r.map((x,i)=>`<tr class="row" data-id="${x.id}">
    <td class="rank">${i+1}</td>
    <td><span class="pbar" style="background:${partyColor(x.party)}"></span>${x.img?`<img class="avatarSm" src="${esc(x.img)}" alt="" loading="lazy">`:''}<span class="nm">${esc(x.name)}</span></td>
    <td class="trendCell">${sparkline(x.series)}</td>
    <td class="scoreCell">${x.score}</td>
    <td>${slopeHTML(x.slope7)}</td>
    <td>${slopeHTML(x.slope30)}</td></tr>`).join('');
}
function earlyTotals(){
  const t={IA:[],NH:[],SC:[],NV:[]};
  records().forEach(x=>{const es=x.early_states||{};Object.keys(t).forEach(s=>{if(es[s])t[s].push({name:x.name,id:x.id,n:es[s]});});});
  Object.keys(t).forEach(s=>t[s].sort((a,b)=>b.n-a.n));
  return t;
}
function renderEarlyStates(){
  const el=$('#bigboard'); if(!el) return;
  const t=earlyTotals();
  const max=Math.max(1,...Object.keys(EARLY_NAMES).map(s=>t[s].reduce((a,x)=>a+x.n,0)));
  el.innerHTML=Object.keys(EARLY_NAMES).map(s=>{
    const rows=t[s], tot=rows.reduce((a,x)=>a+x.n,0);
    const list=rows.slice(0,5).map(x=>`<div class="sc-row" data-id="${x.id}"><span>${esc(x.name)}</span><span>${x.n}</span></div>`).join('')||'<div class="sc-row" style="color:var(--muted)">No tracked activity yet</div>';
    return `<div class="statecard" data-state="${s}" role="button" tabindex="0" aria-label="${EARLY_NAMES[s]}: ${tot} activity signals; filter the board to candidates active here">
      <h3>${EARLY_NAMES[s]} <span class="sc-n">${tot}</span></h3>
      <div class="heatbar"><i style="width:${Math.round(tot/max*100)}%"></i></div>
      <div class="sc-list">${list}</div></div>`;
  }).join('');
}
function renderStateFilter(){
  const el=$('#stateFilter'); if(!el) return;
  el.innerHTML=(PREFS.earlyState&&PREFS.view==='board')
    ? `<div class="statefilter">Showing candidates active in <b>${EARLY_NAMES[PREFS.earlyState]||PREFS.earlyState}</b><span class="x" id="clearState" title="Clear early-state filter" role="button" tabindex="0">×</span></div>` : '';
}
function renderCompare(){
  $('#compareCount').textContent=COMPARE.length||'';
  $('#cmpMeta').textContent=COMPARE.length?COMPARE.length+' selected':'';
  const recs=COMPARE.map(id=>DB.records.find(r=>r.id===id)).filter(Boolean).map(enrich);
  const body=$('#cmpBody'); if(!body) return;
  if(recs.length<2){ body.innerHTML=`<div class="cmpEmpty">Pick 2–3 candidates to compare — tick <b>compare</b> on any card, or open a candidate and click <b>＋ Compare</b>.${recs.length?'<br><br>Selected: '+recs.map(r=>esc(r.name)).join(', '):''}</div>`; return; }
  const rows=[
    ['Status',r=>`<span class="tier" style="background:${TIERS[r.tier].c}">${esc(r.statusLabel)}</span>`],
    ['Momentum',r=>`<b>${r.score}</b>`],
    ['Trend',r=>sparkline(r.series)],
    ['Δ 7‑day',r=>slopeHTML(r.slope7)],
    ['Δ 30‑day',r=>slopeHTML(r.slope30)],
    ['Lane',r=>esc(r.party)],
    ['Confidence',r=>esc(r.conf)],
    ['Early states',r=>{const es=r.early_states||{};const k=Object.keys(es);return k.length?k.map(s=>esc(s)+' '+es[s]).join(' · '):'—';}],
    ['Money (receipts)',r=>r.money?fmtMoney(r.money.receipts):'<span class="money-na">not filed</span>'],
    ['Latest signal',r=>esc(r.headline)],
  ];
  body.innerHTML=`<div class="cmpgrid">${recs.map(r=>`<div class="cmpcol"><h3>${esc(r.name)} <span class="x" data-cmpx="${r.id}" role="button" tabindex="0" style="float:right;cursor:pointer;color:var(--muted)" title="Remove">×</span></h3>${rows.map(kv=>`<div class="crow"><span class="ck">${kv[0]}</span><span>${kv[1](r)}</span></div>`).join('')}</div>`).join('')}</div>`;
}
function renderCmpTray(){
  const el=$('#cmptray'); if(!el) return;
  if(!COMPARE.length){el.classList.add('hide');el.innerHTML='';return;}
  const names=COMPARE.map(id=>{const r=DB.records.find(x=>x.id===id);return r?{id,name:r.name}:null;}).filter(Boolean);
  el.innerHTML=`<span>Compare:</span>${names.map(n=>`<span class="cchip">${esc(n.name)}<span class="x" data-cmpx="${n.id}" role="button" tabindex="0" title="Remove">×</span></span>`).join('')}<button class="btn" id="openCompare" style="background:#fff">Compare ${names.length}</button><button class="btn" id="clearCompare" style="background:transparent;color:#fff;border-color:rgba(255,255,255,.4)">Clear</button>`;
  el.classList.remove('hide');
}
function renderAll(){renderStats();renderQuick();renderBoard();renderCards();renderReview();renderFeed();renderLegend();renderBriefing();renderTrajectory();renderEarlyStates();renderCompare();renderCmpTray();renderStateFilter();syncControls();if(window.__hirRadar)window.__hirRadar.plot();}

/* ---------- drawer ---------- */
function openDrawer(id){
  const c=DB.records.find(x=>x.id===id); if(!c)return;
  const x=enrich(c); const bd=breakdown(c);
  const total=x.score;
  const es=x.early_states||{}, esk=Object.keys(es); const m=x.money;
  $('#drawerInner').innerHTML=`
    <div class="dh">
      <span class="x" data-close>×</span>
      ${x.img?`<img class="dhPortrait" src="${esc(x.img)}" alt="${esc(x.name)}">`:''}
      <h2 style="font-size:20px">${esc(x.name)}</h2>
      <div style="color:var(--muted);font-size:12.5px;margin-top:3px">${esc(x.role)} · <span style="color:${partyColor(x.party)};font-weight:600">${esc(x.party)}</span></div>
      <div style="margin-top:9px">${tierBadge(x)} ${confHTML(x.conf)} ${deltaHTML(x.delta)}${x.pollLead?` <span class="pollLead">★ ${esc(x.pollLead)}</span>`:''}</div>
    </div>
    <div class="body">
      <div class="kv">
        <div class="k">Latest signal</div><div>${esc(x.headline)}</div>
        <div class="k">Why it matters</div><div>${esc(x.why)}</div>
        <div class="k">Last signal</div><div>${new Date(x.lastSignal).toLocaleDateString('en-US',{year:'numeric',month:'long',day:'numeric'})} · ${daysSince(x.lastSignal)} days ago</div>
        <div class="k">Bucket</div><div>${({formal:'Formal (declared/exploratory)',considering:'Actively considering',soft:'Soft / positioning',edge:'Edge / conditional',out:'Ruled out / inactive'})[x.bucket]}</div>
      </div>
      ${x.quote?`<div class="quote">"${esc(x.quote)}"</div>`:''}
      <div class="break">
        <h4>Why this momentum score?</h4>
        ${bd.map(b=>`<div class="cmp"><span>${b.label}</span><span class="v ${b.w>=0?'pos':'neg'}">${b.w>=0?'+':''}${b.w}</span></div>`).join('')||'<div class="cmp"><span>No active signals</span><span class="v">0</span></div>'}
        <div class="cmp total"><span>Momentum (capped 0–100)</span><span class="v">${total}</span></div>
      </div>
      <div class="break">
        <h4>Momentum trajectory</h4>
        <div style="display:flex;align-items:center;gap:10px">${sparkline(x.series)}<span class="conf">Δ7d ${slopeHTML(x.slope7)} · Δ30d ${slopeHTML(x.slope30)}</span></div>
        ${tierTimeline(x.series)}
      </div>
      ${esk.length?`<div class="break"><h4>Early-state activity</h4>${esk.map(s=>`<div class="cmp"><span>${EARLY_NAMES[s]||s}</span><span class="v">${es[s]} signal${es[s]>1?'s':''}</span></div>`).join('')}</div>`:''}
      <div class="break">
        <h4>Money movement <span style="font-weight:400;text-transform:none;color:var(--muted)">(FEC · separate axis)</span></h4>
        ${m?`<table class="money-tbl"><tr><td>Receipts</td><td>${fmtMoney(m.receipts)}</td></tr><tr><td>Disbursements</td><td>${fmtMoney(m.disbursements)}</td></tr><tr><td>Cash on hand</td><td>${fmtMoney(m.cash_on_hand)}</td></tr><tr><td>Debts</td><td>${fmtMoney(m.debts)}</td></tr></table><div class="conf" style="margin-top:6px">${esc(m.committee||'')}${m.coverage_end?' · thru '+esc(m.coverage_end):''}${safeUrl(m.source)?` · <a href="${esc(m.source)}" target="_blank" rel="noopener">FEC ↗</a>`:''}</div>`:`<div class="money-na">No FEC financial report on file (not a registered filer, or no totals yet). Money is never folded into momentum.</div>`}
      </div>
      ${x.tags&&x.tags.length?`<div class="tagrow">${x.tags.map(t=>`<span class="tag">${esc(t)}</span>`).join('')}</div>`:''}
      <div style="margin-top:18px;display:flex;gap:8px;flex-wrap:wrap">
        <button class="btn" data-cmpadd="${x.id}">${COMPARE.includes(x.id)?'✓ In compare':'＋ Compare'}</button>
        <button class="btn" data-edit="${x.id}">Edit</button>
        <button class="btn" data-del="${x.id}">Delete</button>
      </div>
      <div class="note" style="margin-top:14px">Momentum = breadth & recency of activity. Status tier = furthest verifiable step taken. They move independently — a downplaying front-runner can sit at "Floated" with mid momentum.</div>
    </div>`;
  showDrawer();
}
const ALLKEYS=['declared','exploratory','consideringQuote','softConsidering','earlyState','donors','staffing','mediaBlitz','endorsedOther','ruledOut','barred'];
function editForm(id){
  // anticipatory defaults for a NEW candidate: inherit the lane/bucket the user is
  // currently filtering by, and stamp today's date — fewer fields to touch.
  const c=id?DB.records.find(x=>x.id===id):{id:'',name:'',party:(PREFS.party.length===1?PREFS.party[0]:'Democrat'),role:'',bucket:(PREFS.bucket||'considering'),keys:[],conf:'Medium',delta:0,lastSignal:TODAY.toISOString().slice(0,10),headline:'',why:'',quote:'',tags:[]};
  $('#drawerInner').innerHTML=`
    <div class="dh"><span class="x" data-close>×</span><h2 style="font-size:19px">${id?'Edit':'Add'} candidate</h2></div>
    <div class="body form">
      <label>Name</label><input type="text" id="f-name" value="${esc(c.name)}">
      <label>Role / current office</label><input type="text" id="f-role" value="${esc(c.role)}">
      <div style="display:flex;gap:10px">
        <div style="flex:1"><label>Party / lane</label><select id="f-party">${['Democrat','Republican','Libertarian','Independent'].map(p=>`<option ${c.party===p?'selected':''}>${p}</option>`).join('')}</select></div>
        <div style="flex:1"><label>Bucket</label><select id="f-bucket">${[['formal','Formal'],['considering','Considering'],['soft','Soft/positioning'],['edge','Edge'],['out','Ruled out']].map(([v,l])=>`<option value="${v}" ${c.bucket===v?'selected':''}>${l}</option>`).join('')}</select></div>
      </div>
      <label>Signals present (drive status + score)</label>
      <div class="checks">${ALLKEYS.map(k=>`<label><input type="checkbox" class="f-key" value="${k}" ${c.keys.includes(k)?'checked':''}> ${WEIGHTS[k].label} <span style="color:var(--muted)">(${WEIGHTS[k].w>0?'+':''}${WEIGHTS[k].w})</span></label>`).join('')}</div>
      <div style="display:flex;gap:10px">
        <div style="flex:1"><label>Confidence</label><select id="f-conf">${Object.keys(CONF).map(k=>`<option ${c.conf===k?'selected':''}>${k}</option>`).join('')}</select></div>
        <div style="flex:1"><label>7-day delta</label><input type="text" id="f-delta" value="${c.delta||0}"></div>
        <div style="flex:1"><label>Last signal</label><input type="text" id="f-date" value="${c.lastSignal}"></div>
      </div>
      <label>Latest signal (headline)</label><input type="text" id="f-headline" value="${esc(c.headline)}">
      <label>Why it matters</label><textarea id="f-why">${esc(c.why)}</textarea>
      <label>Best quote (optional)</label><textarea id="f-quote">${esc(c.quote)}</textarea>
      <label>Tags (comma-separated)</label><input type="text" id="f-tags" value="${esc((c.tags||[]).join(', '))}">
      <div class="livescore"><span>Live status / momentum</span><span id="f-live" style="font-weight:700"></span></div>
      <div style="display:flex;gap:8px;margin-top:12px">
        <button class="btn primary" data-save="${id||''}">Save</button>
        <button class="btn" data-close>Cancel</button>
      </div>
    </div>`;
  showDrawer();
  const upd=()=>{
    const keys=[...document.querySelectorAll('.f-key:checked')].map(e=>e.value);
    const tmp={keys,lastSignal:$('#f-date').value};
    const st=deriveStatus(keys);
    $('#f-live').innerHTML=`<span class="tier" style="background:${TIERS[st.tier].c}">${st.label}</span> · ${score(tmp)}`;
  };
  document.querySelectorAll('.f-key').forEach(e=>e.addEventListener('change',upd));
  $('#f-date').addEventListener('input',upd); upd();
}
function saveForm(id){
  const nameEl=$('#f-name'); const nm=nameEl.value.trim();
  if(!nm){ shake(nameEl); nameEl.classList.add('f-err'); nameEl.focus&&nameEl.focus(); return; }  // error feedback
  nameEl.classList.remove('f-err');
  const keys=[...document.querySelectorAll('.f-key:checked')].map(e=>e.value);
  const rec={
    id:id||('c'+Date.now()),
    name:nm,
    role:$('#f-role').value.trim(),
    party:$('#f-party').value, bucket:$('#f-bucket').value,
    keys, conf:$('#f-conf').value, delta:parseInt($('#f-delta').value)||0,
    lastSignal:$('#f-date').value, headline:$('#f-headline').value.trim(),
    why:$('#f-why').value.trim(), quote:$('#f-quote').value.trim(),
    tags:$('#f-tags').value.split(',').map(s=>s.trim()).filter(Boolean)
  };
  const i=DB.records.findIndex(x=>x.id===id);
  if(i>=0)DB.records[i]=rec; else DB.records.push(rec);
  save(); renderAll(); closeDrawer(); toast(id?'Saved':'Added'); flashRow(rec.id);   // success feedback
}
function delRec(id){ DB.records=DB.records.filter(x=>x.id!==id); save(); renderAll(); closeDrawer(); toast('Removed'); }

function showDrawer(){$('#drawer').classList.add('on');$('#scrim').classList.add('on');}
function closeDrawer(){$('#drawer').classList.remove('on');$('#scrim').classList.remove('on');}

/* ---------- controls ---------- */
function syncControls(){
  $('#q').value=PREFS.q||'';
  $('#bucketSel').value=PREFS.bucket||'';
  $('#confSel').value=PREFS.conf||'';
  document.querySelectorAll('#viewSeg button').forEach(b=>{const on=b.dataset.view===PREFS.view;b.classList.toggle('on',on);b.setAttribute('aria-selected',on?'true':'false');});
  const V=PREFS.view;
  $('#boardPanel').classList.toggle('hide',V!=='board');
  $('#cardsPanel').classList.toggle('hide',V!=='cards');
  $('#trajPanel').classList.toggle('hide',V!=='traj');
  $('#earlyPanel').classList.toggle('hide',V!=='early');
  $('#comparePanel').classList.toggle('hide',V!=='compare');
  $('#reviewPanel').classList.toggle('hide',V!=='review');
  $('#quickbar').classList.toggle('hide',!(V==='board'||V==='cards'));
  document.querySelectorAll('.chip.party').forEach(c=>c.classList.toggle('on',PREFS.party.includes(c.dataset.v)));
  renderStateFilter();
}
function persistPrefs(){DB.prefs=PREFS;save();}

function init(){
  // party chips
  $('#partyChips').innerHTML=['Democrat','Republican','Libertarian','Independent'].map(p=>`<span class="chip party" data-v="${p}">${p.slice(0,3)}</span>`).join('');
  document.querySelectorAll('.chip.party').forEach(c=>c.addEventListener('click',()=>{
    const v=c.dataset.v; const i=PREFS.party.indexOf(v);
    if(i>=0)PREFS.party.splice(i,1); else PREFS.party.push(v);
    pop(c); persistPrefs();renderBoard();renderCards();syncControls();
  }));
  $('#q').addEventListener('input',e=>{PREFS.q=e.target.value;persistPrefs();renderBoard();renderCards();});
  $('#q').addEventListener('change',e=>{const v=e.target.value.trim(); if(v.length>=2){UX.recentSearches=[v].concat((UX.recentSearches||[]).filter(s=>s!==v)).slice(0,5);saveUX();renderHints();}});
  $('#bucketSel').addEventListener('change',e=>{PREFS.bucket=e.target.value;persistPrefs();renderBoard();renderCards();});
  $('#confSel').addEventListener('change',e=>{PREFS.conf=e.target.value;persistPrefs();renderBoard();renderCards();});
  document.querySelectorAll('#viewSeg button').forEach(b=>b.addEventListener('click',()=>{PREFS.view=b.dataset.view;persistPrefs();if(b.dataset.view==='compare')renderCompare();if(b.dataset.view==='traj')renderTrajectory();if(b.dataset.view==='early')renderEarlyStates();syncControls();fadePanel();}));
  document.querySelectorAll('#trajSeg button').forEach(b=>b.addEventListener('click',()=>{PREFS.trajWin=+b.dataset.win;persistPrefs();renderTrajectory();}));
  // keyboard activation for role=button elements (state cards, quick chips, remove ×)
  document.body.addEventListener('keydown',e=>{
    if((e.key==='Enter'||e.key===' ')&&e.target&&e.target.getAttribute&&e.target.getAttribute('role')==='button'){e.preventDefault();e.target.click();}
  });
  // compare deep-link: react to #compare= changes (back/forward, shared links)
  window.addEventListener('hashchange',()=>{COMPARE=parseCompareHash();renderCmpTray();renderCards();if(PREFS.view==='compare')renderCompare();});
  document.querySelectorAll('#board th[data-sort]').forEach(th=>th.addEventListener('click',()=>{
    const s=th.dataset.sort;
    if(PREFS.sort===s)PREFS.dir=PREFS.dir==='asc'?'desc':'asc';
    else{PREFS.sort=s;PREFS.dir=(s==='name'||s==='party')?'asc':'desc';}
    persistPrefs();renderBoard();
  }));
  $('#addBtn').addEventListener('click',()=>editForm(null));
  $('#exportBtn').addEventListener('click',()=>{
    const blob=new Blob([JSON.stringify(DB.records,null,2)],{type:'application/json'});
    const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='hat-in-ring-2028.json';a.click();
    toast('Exported '+DB.records.length+' records');
  });
  $('#exportDecisionsBtn').addEventListener('click',()=>{
    const out=Object.entries(DECISIONS).map(([rid,action])=>({rid,action}));
    const blob=new Blob([JSON.stringify(out,null,2)],{type:'application/json'});
    const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='review_decisions.json';a.click();
    toast(out.length?'Exported '+out.length+' decision'+(out.length>1?'s':''):'No decisions yet');
  });
  // delegated clicks
  document.body.addEventListener('click',e=>{
    const row=e.target.closest('[data-id]');
    if(e.target.closest('[data-close]')){closeDrawer();return;}
    if(e.target.closest('.cmp-pick')){toggleCompare(e.target.closest('.cmp-pick').dataset.cmp);return;}
    if(e.target.closest('[data-cmpadd]')){const id=e.target.closest('[data-cmpadd]').dataset.cmpadd;toggleCompare(id);openDrawer(id);return;}
    if(e.target.closest('[data-cmpx]')){toggleCompare(e.target.closest('[data-cmpx]').dataset.cmpx);return;}
    if(e.target.closest('#openCompare')){PREFS.view='compare';persistPrefs();renderCompare();syncControls();fadePanel();return;}
    if(e.target.closest('#clearCompare')){clearCompare();return;}
    if(e.target.closest('#clearState')){PREFS.earlyState='';persistPrefs();renderAll();return;}
    if(e.target.closest('.sc-row[data-id]')){openDrawer(e.target.closest('.sc-row').dataset.id);return;}
    if(e.target.closest('[data-state]')){PREFS.earlyState=e.target.closest('[data-state]').dataset.state;PREFS.view='board';persistPrefs();renderAll();fadePanel();toast('Filtered to '+(EARLY_NAMES[PREFS.earlyState]||PREFS.earlyState));return;}
    if(e.target.closest('[data-quick]')){const k=e.target.closest('[data-quick]').dataset.quick;PREFS.quick=(PREFS.quick===k?'':k);persistPrefs();renderAll();return;}
    if(e.target.closest('#showMovers')){PREFS.quick='movers';PREFS.view='board';persistPrefs();$('#nudge').classList.add('hide');renderAll();fadePanel();return;}
    if(e.target.closest('#nudgeX')){UX.dismissedNudges[GENERATED_AT]=true;saveUX();$('#nudge').classList.add('hide');return;}
    if(e.target.closest('#resetPrefs')){PREFS=Object.assign({view:'board',party:[],bucket:'',conf:'',q:'',sort:'score',dir:'desc',quick:''});DB.prefs=PREFS;save();renderAll();toast('View preferences reset');return;}
    if(e.target.closest('[data-confirm]')){decide(e.target.closest('[data-confirm]').dataset.confirm,'confirm');return;}
    if(e.target.closest('[data-dismiss]')){decide(e.target.closest('[data-dismiss]').dataset.dismiss,'dismiss');return;}
    if(e.target.closest('[data-save]')!==null){saveForm(e.target.closest('[data-save]').dataset.save||'');return;}
    if(e.target.closest('[data-edit]')){editForm(e.target.closest('[data-edit]').dataset.edit);return;}
    if(e.target.closest('[data-del]')){if(confirm('Remove this candidate?'))delRec(e.target.closest('[data-del]').dataset.del);return;}
    if(row&&!e.target.closest('.drawer')){openDrawer(row.dataset.id);}
  });
  $('#scrim').addEventListener('click',closeDrawer);
  document.addEventListener('keydown',e=>{if(e.key==='Escape')closeDrawer();});

  $('#foot').innerHTML=`Manually-maintained tracker — data seeded from public reporting (AP, Reuters, Time, WaPo, ABC, C-SPAN, FEC) as of {{ as_of }} (auto-generated {{ generated_at_human }}). Edits/additions save to this browser only; use <b>Export</b> to back up or port to Notion. <b>Status tier</b> = furthest verifiable step taken; <b>momentum</b> = weighted activity + recency, capped 0–100. Not a live FEC/news feed — see chat notes for wiring options. · <span class="resetlink" id="resetPrefs">Reset view</span>`;
  // drawer swipe-to-dismiss (touch): light resistance + snap on release
  (function(){var d=$('#drawer'),x0=null;
    d.addEventListener('touchstart',function(ev){x0=ev.touches[0].clientX;d.style.transition='none';},{passive:true});
    d.addEventListener('touchmove',function(ev){if(x0==null)return;var dx=Math.max(0,ev.touches[0].clientX-x0);d.style.transform='translateX('+(dx*0.7)+'px)';},{passive:true});
    d.addEventListener('touchend',function(ev){if(x0==null)return;var dx=ev.changedTouches[0].clientX-x0;d.style.transition='';d.style.transform='';if(dx>90)closeDrawer();x0=null;});
  })();
  if(COMPARE.length) PREFS.view='compare';         // arrived via a #compare= deep link
  renderAll(); renderNudge(); renderHints();
  if(window.__hirRadar)window.__hirRadar.boot();   // mount + start the radar scope (no-op headless)
  UX.lastSeenVer=GENERATED_AT; saveUX();          // mark this build as seen
}

/* ===== Hat-in-Ring Radar scope (design graft) — self-contained & headless-safe.
   Reads the live filtered()/records() + PARTYC/TIERS/CONF/esc globals; blips carry
   data-id so the existing delegated click handler opens the candidate drawer. ===== */
(function(){
  var PARTY_ANGLE={Democrat:205,Republican:-25,Libertarian:70,Independent:140};   // degrees
  var RADAR={on:true,beam:0,period:6000,last:0,raf:0};
  var EL,CV,CTX,BLIPS,PLOT=[],SW=0,SH=0,built=false;
  function q(s){return document.querySelector(s);}
  function build(){
    if(built)return;
    EL=q('#scopeHost'); CV=q('#hirScopeCanvas'); BLIPS=q('#hirBlips');
    if(!CV||typeof CV.getContext!=='function'||!BLIPS)return;   // headless / no-canvas: stay un-built
    CTX=CV.getContext('2d'); built=true;
    var leg=q('#hirScopeLegend');
    if(leg)leg.innerHTML=['Democrat','Republican','Libertarian','Independent'].map(function(p){
      return '<span class="lm" style="color:'+(PARTYC[p]||'var(--other)')+'"><i style="background:'+(PARTYC[p]||'var(--other)')+'"></i>'+p.slice(0,3)+'</span>';}).join('');
    fit();
  }
  function fit(){
    if(!CV||!CTX)return;
    var box=CV.parentElement; if(!box)return;
    var w=box.clientWidth,h=box.clientHeight; if(!w||!h)return;
    var dpr=Math.min(window.devicePixelRatio||1,2);
    CV.width=Math.round(w*dpr); CV.height=Math.round(h*dpr);
    CTX.setTransform(dpr,0,0,dpr,0,0); SW=w; SH=h;
  }
  function geom(){return {cx:SW/2,cy:SH/2,maxR:Math.min(SW,SH)/2*0.9};}
  function plot(){
    if(!built||!BLIPS)return;
    fit();
    var data=(typeof filtered==='function'?filtered():[]).filter(function(x){return x.tier>0;});
    var byParty={}; data.forEach(function(x){(byParty[x.party]=byParty[x.party]||[]).push(x);});
    var g=geom(); PLOT=[];
    Object.keys(byParty).forEach(function(party){
      var list=byParty[party].sort(function(a,b){return b.score-a.score;});
      var base=(PARTY_ANGLE[party]!=null?PARTY_ANGLE[party]:90)*Math.PI/180, span=70*Math.PI/180;
      list.forEach(function(x,i){
        var t=list.length>1?(i/(list.length-1)-0.5):0, ang=base+t*span;
        var rr=g.maxR*(0.13+0.87*(1-x.score/100));
        PLOT.push({x:x,ang:((ang%(2*Math.PI))+2*Math.PI)%(2*Math.PI),px:g.cx+Math.cos(ang)*rr,py:g.cy+Math.sin(ang)*rr});
      });
    });
    BLIPS.innerHTML=PLOT.map(function(p){
      var x=p.x, sz=12+((CONF[x.conf]||0)*2.2);
      var lbl=x.statusLabel||(TIERS[x.tier]&&TIERS[x.tier].label)||'', col=(TIERS[x.tier]&&TIERS[x.tier].c)||'#fff';
      return '<div class="hir-blip'+(x.pollLead?' lead':'')+'" data-id="'+x.id+'" data-blip="'+x.id+'" tabindex="0" role="button" aria-label="'+esc(x.name)+'"'
        +' style="left:'+(p.px/SW*100).toFixed(2)+'%;top:'+(p.py/SH*100).toFixed(2)+'%;--bc:'+(PARTYC[x.party]||'var(--other)')+';--bs:'+sz.toFixed(0)+'px">'
        +(x.pollLead?'<span class="star">★</span>':'')
        +'<div class="hir-tip"><div class="bn">'+esc(x.name)+'</div><div class="br">'+esc(lbl)+' · '+esc(x.party)+'</div><div>Momentum <b style="color:'+col+'">'+x.score+'</b></div></div>'
        +'</div>';
    }).join('');
    draw();
  }
  function draw(){
    if(!CTX||!SW)return;
    var g=geom(), ctx=CTX, sky='rgba(186,202,239,';
    ctx.clearRect(0,0,SW,SH); ctx.lineWidth=1;
    for(var i=1;i<=4;i++){ctx.beginPath();ctx.arc(g.cx,g.cy,g.maxR*i/4,0,2*Math.PI);ctx.strokeStyle=sky+(i===4?0.22:0.13)+')';ctx.stroke();}
    for(var a=0;a<360;a+=30){var r=a*Math.PI/180;ctx.beginPath();ctx.moveTo(g.cx,g.cy);ctx.lineTo(g.cx+Math.cos(r)*g.maxR,g.cy+Math.sin(r)*g.maxR);ctx.strokeStyle=sky+'0.07)';ctx.stroke();}
    ctx.beginPath();ctx.arc(g.cx,g.cy,3,0,2*Math.PI);ctx.fillStyle='rgba(206,32,41,.9)';ctx.fill();
    if(RADAR.on){
      var b=RADAR.beam, grad=ctx.createConicGradient?ctx.createConicGradient(b,g.cx,g.cy):null;
      ctx.save();ctx.beginPath();ctx.moveTo(g.cx,g.cy);ctx.arc(g.cx,g.cy,g.maxR,b-0.5,b);ctx.closePath();
      if(grad){grad.addColorStop(0,'rgba(206,32,41,0)');grad.addColorStop(0.92,'rgba(206,32,41,0)');grad.addColorStop(1,'rgba(206,32,41,.30)');ctx.fillStyle=grad;}
      else ctx.fillStyle='rgba(206,32,41,.18)';
      ctx.fill();
      ctx.beginPath();ctx.moveTo(g.cx,g.cy);ctx.lineTo(g.cx+Math.cos(b)*g.maxR,g.cy+Math.sin(b)*g.maxR);ctx.strokeStyle='rgba(255,70,66,.85)';ctx.lineWidth=1.5;ctx.stroke();
      ctx.restore();
    }
  }
  function frame(ts){
    if(!RADAR.on)return;
    if(!RADAR.last)RADAR.last=ts;
    var dt=ts-RADAR.last; RADAR.last=ts;
    RADAR.beam=(RADAR.beam+(2*Math.PI)*(dt/RADAR.period))%(2*Math.PI);
    draw();
    var beam=RADAR.beam;
    PLOT.forEach(function(p){
      var d=Math.abs(((p.ang-beam+Math.PI)%(2*Math.PI))-Math.PI);
      var el=BLIPS&&BLIPS.querySelector('[data-blip="'+p.x.id+'"]'); if(!el)return;
      if(d<0.10){if(!el._pinged){el.classList.add('ping');el._pinged=1;}}
      else if(el._pinged&&d>0.18){el.classList.remove('ping');el._pinged=0;}
    });
    RADAR.raf=requestAnimationFrame(frame);
  }
  function start(){
    if(typeof requestAnimationFrame!=='function'){try{draw();}catch(e){}return;}
    if(typeof cancelAnimationFrame==='function')cancelAnimationFrame(RADAR.raf);
    RADAR.last=0;
    var reduce=window.matchMedia&&window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    if(RADAR.on&&!reduce)RADAR.raf=requestAnimationFrame(frame); else draw();
  }
  function setRadar(on){RADAR.on=on;if(EL)EL.classList.toggle('radar-off',!on);start();}
  function setDir(dir){
    if(EL)EL.setAttribute('data-dir',dir);
    var dd=document.querySelectorAll('.hir-dirbtn');
    for(var i=0;i<dd.length;i++)dd[i].classList.toggle('on',dd[i].getAttribute('data-hirdir')===dir);
    var ds=q('#hirDirSel'); if(ds)ds.textContent=dir.charAt(0).toUpperCase()+dir.slice(1);
    fit(); plot();
  }
  function boot(){
    build();
    if(!built)return;
    var dd=document.querySelectorAll('.hir-dirbtn');
    for(var i=0;i<dd.length;i++)(function(btn){btn.addEventListener('click',function(){setDir(btn.getAttribute('data-hirdir'));});})(dd[i]);
    var tb=q('#hirTweakBtn'),tw=q('#hirTweaks');
    if(tb&&tw)tb.addEventListener('click',function(){tw.hidden=!tw.hidden;});
    var ro=q('#hirRadarOn'); if(ro)ro.addEventListener('click',function(){var on=!RADAR.on;setRadar(on);ro.textContent=on?'On':'Off';ro.classList.toggle('on',on);});
    var sp=q('#hirSpeed'); if(sp)sp.addEventListener('input',function(e){RADAR.period=(13-(+e.target.value))*1000;if(RADAR.on)start();});
    var ds=q('#hirDirSel'); if(ds)ds.addEventListener('click',function(){setDir(EL.getAttribute('data-dir')==='command'?'briefing':'command');});
    var rt=null; window.addEventListener('resize',function(){clearTimeout(rt);rt=setTimeout(function(){fit();plot();},120);});
    plot(); start();
  }
  window.__hirRadar={boot:boot,plot:plot,build:build,start:start,setRadar:setRadar};
})();
init();
</script>
</body>
</html>
