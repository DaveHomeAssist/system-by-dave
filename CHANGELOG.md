# Changelog

## 2026-06-14

* Added `/client-signoff.html`, a browser based AV client sign off tracker with show metadata, sample closeout acceptance items, editable rows, area item approver due status signed time exception owner and notes fields, area and status filters, selected sign off preview, selected sign off controls, pending ready signed exception follow up waived and rejected status, total signed pending exception follow up and ready counts, sign off gap warnings, print output, JSON import and export, CSV export, copyable operator summary, remembered browser state, keyboard shortcuts, tactile button feedback, and mobile table containment. Linked it from the homepage AV Tools section, added it to the Products footer, and added it to `sitemap.xml`.
* Added `/change-order.html`, a browser based AV change order log with show metadata, sample client change requests, editable rows, area request requester requested time impact cost approval owner due status and notes fields, area and status filters, selected change preview, selected change controls, requested pricing approved in progress completed billed declined and issue status, total approved pending open risk and value counts, approval gap warnings, print output, JSON import and export, CSV export, copyable operator summary, remembered browser state, keyboard shortcuts, tactile button feedback, and mobile table containment. Linked it from the homepage AV Tools section, added it to the Products footer, and added it to `sitemap.xml`.
* Added `/crew-time-log.html`, a browser based AV crew time log with show metadata, sample crew entries, editable rows, department name role call actual check in meal minutes release time computed hours status issue and notes fields, department and status filters, selected crew preview, selected crew controls, scheduled confirmed checked in on clock meal wrapped released issue and no show status, total checked active wrapped issue and hour counts, crew time gap warnings, print output, JSON import and export, CSV export, copyable operator summary, remembered browser state, keyboard shortcuts, tactile button feedback, and mobile table containment. Linked it from the homepage AV Tools section, added it to the Products footer, and added it to `sitemap.xml`.
* Added `/show-report.html`, a browser based AV show report with show metadata, sample report entries, editable rows, time type area subject owner severity status follow up details and resolution fields, type status and severity filters, selected report entry preview, selected entry controls, logged watching active escalated resolved follow up closed and deferred status, total closed open risk follow up and area counts, report gap warnings, print output, JSON import and export, CSV export, copyable operator summary, remembered browser state, keyboard shortcuts, tactile button feedback, and mobile table containment. Linked it from the homepage AV Tools section, added it to the Products footer, and added it to `sitemap.xml`.
* Added `/truck-pack.html`, a browser based AV truck pack plan with show metadata, sample truck pack items, editable rows, department case contents truck zone load order unload order weight owner status issue and notes fields, department and status filters, selected truck pack item preview, selected item controls, planned pulled staged packed strapped loaded issue and hold status, total ready open issue weight and zone counts, pack gap warnings, print output, JSON import and export, CSV export, copyable operator summary, remembered browser state, keyboard shortcuts, tactile button feedback, and mobile table containment. Linked it from the homepage AV Tools section, added it to the Products footer, and added it to `sitemap.xml`.
* Added `/site-survey.html`, a browser based AV site survey with show metadata, sample venue survey checks, editable rows, area check owner due priority status finding follow up and notes fields, area and status filters, selected survey check preview, selected check controls, unknown clear needs info blocked confirmed and deferred status, total clear open blocked follow up and area counts, survey gap warnings, print output, JSON import and export, CSV export, copyable operator summary, remembered browser state, keyboard shortcuts, tactile button feedback, and mobile table containment. Linked it from the homepage AV Tools section, added it to the Products footer, and added it to `sitemap.xml`.
* Clarified the `/teleprompter.html` controls hide affordance at compact widths by giving the header action a visible Hide or Show label, preserving the `H` shortcut, and keeping the main toolbar hide button wide enough to read.
* Added `/load-in-plan.html`, a browser based AV load in plan with show metadata, sample load in plan, editable rows, department item truck dock destination owner due status blocker and notes fields, department and status filters, selected load in item preview, selected item controls, planned staged arrived delivered built checked issue and hold status, total ready open issue gap and owner counts, load in gap warnings, print output, JSON import and export, CSV export, copyable operator summary, remembered browser state, keyboard shortcuts, tactile button feedback, and mobile table containment. Linked it from the homepage AV Tools section, added it to the Products footer, and added it to `sitemap.xml`.
* Added `/cable-plan.html`, a browser based AV cable plan with show metadata, sample cable runs, editable rows, type cable length source destination path label owner status issue and notes fields, type and status filters, selected cable preview, selected cable controls, planned pulled labeled dressed tested issue and spare status, total tested open issue gap and owner counts, cable gap warnings, print output, JSON import and export, CSV export, copyable operator summary, remembered browser state, keyboard shortcuts, tactile button feedback, and mobile table containment. Linked it from the homepage AV Tools section, added it to the Products footer, and added it to `sitemap.xml`.
* Made the `/teleprompter.html` controls hide action harder to miss by adding a second Hide controls button inside the main play control group while keeping the header control, `H` shortcut, floating Show controls restore button, and remembered collapsed state.
* Added `/strike-plan.html`, a browser based AV strike and load out tracker with show metadata, sample strike plan, editable rows, department item location owner case destination status issue and notes fields, department and status filters, selected strike item preview, selected item controls, pending struck packed loaded returned issue missing and hold status, total complete open issue gap and case counts, strike gap warnings, print output, JSON import and export, CSV export, copyable operator summary, remembered browser state, keyboard shortcuts, tactile button feedback, and mobile table containment. Linked it from the homepage AV Tools section, added it to the Products footer, and added it to `sitemap.xml`.
* Added `/room-check.html`, a browser based AV room readiness tracker with show metadata, sample room check, editable rows, area check owner due priority status blocker and notes fields, area and status filters, selected check preview, selected check controls, pending checked ready watch issue deferred and N A status, total ready open issue gap and owner counts, readiness gap warnings, print output, JSON import and export, CSV export, copyable operator summary, remembered browser state, keyboard shortcuts, tactile button feedback, and mobile table containment. Linked it from the homepage AV Tools section, added it to the Products footer, and added it to `sitemap.xml`.
* Added `/line-check.html`, a browser based AV line check tracker with show metadata, sample line check, editable rows, channel source type location tech console input destination talkback status problem and notes fields, type and status filters, selected line preview, selected line controls, pending patched checked passed issue muted spare status, total passed open issue check gap and tech counts, check gap warnings, print output, JSON import and export, CSV export, copyable operator summary, remembered browser state, keyboard shortcuts, tactile button feedback, and mobile table containment. Linked it from the homepage AV Tools section, added it to the Products footer, and added it to `sitemap.xml`.
* Added `/stream-plan.html`, a browser based AV stream planner with show metadata, sample stream plan, editable rows, encoder stream type platform destination server key label input resolution bitrate audio record backup status and notes fields, stream type and status filters, selected stream preview, selected stream controls, planned configured sending tested ready issue backup status, total ready open issue stream gap and platform counts, stream gap warnings, print output, JSON import and export, CSV export, copyable operator summary, remembered browser state, keyboard shortcuts, tactile button feedback, and mobile table containment. Linked it from the homepage AV Tools section, added it to the Products footer, and added it to `sitemap.xml`.
* Added `/projection-plan.html`, a browser based AV projection planner with show metadata, sample projection plan, editable rows, screen surface size aspect projector lens throw position input route blend backup status and notes fields, surface and status filters, selected projection preview, selected projection controls, planned rigged focused lined up tested ready issue spare status, total ready open issue projection gap and screen counts, projection gap warnings, print output, JSON import and export, CSV export, copyable operator summary, remembered browser state, keyboard shortcuts, tactile button feedback, and mobile table containment. Linked it from the homepage AV Tools section, added it to the Products footer, and added it to `sitemap.xml`.
* Added `/video-patch.html`, a browser based AV video patch sheet with show metadata, sample video patch, editable rows, source source type format connector switcher or router input converter destination route output backup status and notes fields, type and status filters, selected route preview, selected route controls, planned patched routed tested ready issue spare status, total ready open issue route gap and destination counts, route gap warnings, print output, JSON import and export, CSV export, copyable operator summary, remembered browser state, keyboard shortcuts, tactile button feedback, and mobile table containment. Linked it from the homepage AV Tools section, added it to the Products footer, and added it to `sitemap.xml`.
* Added `/speaker-plan.html`, a browser based AV speaker deployment planner with show metadata, sample speaker plan, editable rows, zone speaker type processor output amp cable trim delay coverage status backup and notes fields, type and status filters, selected speaker preview, selected speaker controls, planned patched tuned coverage checked ready issue spare status, total ready open issue coverage gap and zone counts, coverage and route gap warnings, print output, JSON import and export, CSV export, copyable operator summary, remembered browser state, keyboard shortcuts, tactile button feedback, and mobile table containment. Linked it from the homepage AV Tools section, added it to the Products footer, and added it to `sitemap.xml`.
* Tightened the remembered controls mode in `/teleprompter.html`: hiding controls now clears the header, toolbar, and footer from the viewport, leaves a small fixed Show controls button, keeps the `H` shortcut, and remembers the state in browser preferences.
* Added `/audio-patch.html`, a browser based AV audio patch sheet with show metadata, sample audio patch, editable channel rows, channel source type console stagebox input phantom gain destination monitor status and notes fields, type and status filters, selected channel preview, selected channel controls, planned patched gained line check ready issue spare status, total checked open issue patch gap and stagebox counts, patch gap warnings, print output, JSON import and export, CSV export, copyable operator summary, remembered browser state, keyboard shortcuts, tactile button feedback, and mobile table containment. Linked it from the homepage AV Tools section, added it to the Products footer, and added it to `sitemap.xml`.
* Added `/display-plan.html`, a browser based AV display plan with show metadata, sample display plan, editable display rows, display type input processor resolution aspect refresh route backup status and notes fields, type and status filters, selected display preview, selected display controls, planned cabled routed tested ready issue backup status, total ready open issue backup gap and processor counts, backup gap warnings, print output, JSON import and export, CSV export, copyable operator summary, remembered browser state, keyboard shortcuts, tactile button feedback, and mobile table containment. Linked it from the homepage AV Tools section, added it to the Products footer, and added it to `sitemap.xml`.
* Added `/lighting-patch.html`, a browser based AV lighting patch sheet with show metadata, sample lighting patch, editable fixture rows, position fixture unit mode universe address channel dimmer color focus status and notes fields, position and status filters, selected fixture preview, selected fixture controls, planned hung addressed patched focused issue spare status, total ready open issue conflict and universe counts, duplicate address warnings, print output, JSON import and export, CSV export, copyable operator summary, remembered browser state, keyboard shortcuts, tactile button feedback, and mobile table containment. Linked it from the homepage AV Tools section, added it to the Products footer, and added it to `sitemap.xml`.
* Added `/crew-call.html`, a browser based AV crew call sheet with show metadata, sample crew call, editable crew rows, department name role call location meal release phone status and notes fields, department and status filters, selected crew preview, selected crew controls, scheduled confirmed checked in on site wrapped problem status, total checked in open problem lead and department counts, print output, JSON import and export, CSV export, copyable operator summary, remembered browser state, keyboard shortcuts, tactile button feedback, and mobile table containment. Linked it from the homepage AV Tools section, added it to the Products footer, and added it to `sitemap.xml`.
* Added `/show-advance.html`, a browser based AV show advance tracker with show metadata, sample show advance, editable advance rows, section ask owner due priority status details and notes fields, section and status filters, selected item preview, selected item controls, needed requested received confirmed issue deferred status, total confirmed open issue high priority and section counts, print output, JSON import and export, CSV export, copyable operator summary, remembered browser state, keyboard shortcuts, tactile button feedback, and mobile table containment. Linked it from the homepage AV Tools section, added it to the Products footer, and added it to `sitemap.xml`.
* Rolled CueForge and PlotForge into the AV tools suite: renamed the homepage cards and footer links, updated `cue-sheet.html` and `stage-plot.html` metadata and on page labels, added `cueforge.html` and `plotforge.html` route aliases, updated export schemas and filenames to use CueForge and PlotForge naming, updated `CLAUDE.md`, `AGENTS.md`, and bumped sitemap entries.
* Expanded `/teleprompter.html` formatting and saved script controls: added text alignment, more system reader font stacks, anchor desk producer notes and bold card presets, wider spacing options, reusable custom text background and reading line color swatches, script templates, text only saved script loading, and show package support for custom swatches.

## 2026-06-13

* Added `/gear-prep.html`, a browser based AV gear prep checklist with show metadata, sample gear prep, editable item rows, item category quantity case location owner status check and notes fields, category and status filters, selected item preview, selected item controls, needed pulled tested packed loaded issue spare status, total packed loaded issue open and category counts, print output, JSON import and export, CSV export, copyable operator summary, remembered browser state, keyboard shortcuts, tactile button feedback, and mobile table containment. Linked it from the homepage AV Tools section, added it to the Products footer, and added it to `sitemap.xml`.
* Added `/rf-coordination.html`, a browser based AV RF coordination sheet with show metadata, sample RF plan, editable unit rows, unit type owner zone frequency band channel status backup and notes fields, type and status filters, selected unit preview, selected unit controls, planned scanned clean issue spare status, total clean scanned issue conflict and band counts, duplicate and close spacing warnings, print output, JSON import and export, CSV export, copyable operator summary, remembered browser state, keyboard shortcuts, tactile button feedback, and mobile table containment. Linked it from the homepage AV Tools section, added it to the Products footer, and added it to `sitemap.xml`.
* Added `/network-plan.html`, a browser based AV network planner with show metadata, sample network plan, editable device rows, device role zone network IP method VLAN port status backup and notes fields, network and status filters, selected device preview, selected device controls, planned patched online issue spare status, total online static issue network and VLAN counts, print output, JSON import and export, CSV export, copyable operator summary, remembered browser state, keyboard shortcuts, tactile button feedback, and mobile table containment. Linked it from the homepage AV Tools section, added it to the Products footer, and added it to `sitemap.xml`.
* Added `/power-plan.html`, a browser based AV power plan with show metadata, sample circuit plan, editable circuit rows, location load source voltage capacity draw status backup and notes fields, source and status filters, calculated load percentage, calculated headroom, selected circuit preview, selected circuit controls, planned patched hot issue struck status, print output, JSON import and export, CSV export, copyable operator summary, remembered browser state, keyboard shortcuts, tactile button feedback, and mobile table containment. Linked it from the homepage AV Tools section, added it to the Products footer, and added it to `sitemap.xml`.
* Added `/record-log.html`, a browser based AV record log with show metadata, sample record plan, editable record rows, source type format resolution audio media status duration backup and notes fields, type and status filters, selected record preview, selected record controls, armed rolling stopped issue delivered status, print output, JSON import and export, CSV export, copyable operator summary, remembered browser state, keyboard shortcuts, tactile button feedback, and mobile table containment. Linked it from the homepage AV Tools section, added it to the Products footer, and added it to `sitemap.xml`.
* Added `/signal-flow.html`, a browser based AV signal flow planner with show metadata, sample route plan, editable route rows, source format connector processor destination status backup and notes fields, system and status filters, selected route preview, selected route controls, verified pending issue backup status, print output, JSON import and export, CSV export, copyable operator summary, remembered browser state, keyboard shortcuts, tactile button feedback, and mobile table containment. Linked it from the homepage AV Tools section, added it to the Products footer, and added it to `sitemap.xml`.
* Added `/playback-check.html`, a browser based AV playback checklist with show metadata, sample media plan, editable playback rows, cue file type duration aspect audio destination status backup and notes fields, destination and status filters, selected cue controls, ready pending issue played status, play next, print output, JSON import and export, CSV export, copyable operator summary, remembered browser state, and mobile table containment. Linked it from the homepage AV Tools section, added it to the Products footer, and added it to `sitemap.xml`.
* Expanded `/teleprompter.html` formatting and saved library controls again: added more reader font stacks, lighter and black font weights, lowercase and title case modes, extra letter spacing, word spacing, text stroke, more text background and reading line colors, reusable saved looks, saved script tags, tag search and tag sort, and show package support for saved looks and tags.
* Added `/comms-check.html`, a browser based AV comms check tool with show metadata, sample comms plan, editable assignments, intercom radio and IFB systems, channel and status filters, device IDs, battery status, backups, issue flags, selected assignment controls, print output, JSON import and export, CSV export, copyable operator summary, remembered browser state, and mobile table containment. Linked it from the homepage AV Tools section, added it to the Products footer, and added it to `sitemap.xml`.
* Expanded `/teleprompter.html` formatting and saved-script library controls again: added more reader font stacks, letter spacing, text shadow styles, extra text and background swatches, reading line color controls, pinned saved scripts, saved script sorting, saved script copy, saved library counts, and portable show-package support for the new format fields.
* Added `/camera-shot-list.html`, a browser based AV camera shot list with show metadata, sample camera plan, editable shot rows, camera and status filters, selected shot controls, ready and problem status, take next, print output, JSON import and export, CSV export, copyable operator summary, remembered browser state, and mobile table containment. Linked it from the homepage AV Tools section, added it to the Products footer, and added it to `sitemap.xml`.
* Expanded `/teleprompter.html` reader formatting and saved-script management: added format presets, italic reader text, uppercase mode, more text and background swatches, saved script search, rename, duplicate, and a larger browser saved-script library.
* Added `/show-handoff.html`, a browser based AV show handoff builder with show metadata, exact handoff sections, generated summary table, decision matrix, numbered prioritized actions, copy actions, copy matrix, copy full Markdown, print output, JSON import and export, sample handoff, and remembered browser state. Linked it from the homepage AV Tools section, added it to the Products footer, and added it to `sitemap.xml`.
* Tightened `/teleprompter.html` mobile formatting controls so the toolbar scrolls inside its own rail on phone widths, keeps the editor usable, and gives color swatches larger touch targets.
* Added `/stage-plot.html`, a browser based AV stage plot tool with draggable room layout items, sample stage plot, exact position controls, nudge controls, snap to grid, print output, JSON import and export, copyable operator summary, and remembered browser state. Linked it from the homepage AV Tools section, added it to the Products footer, and added it to `sitemap.xml`.
* Added `/input-list.html`, a browser based AV input list and patch sheet with show metadata, sample channels, editable patch rows, line check status, filters, selected channel controls, print output, JSON import and export, CSV export, copyable operator summary, and remembered browser state. Linked it from the homepage AV Tools section, added it to the Products footer, and added it to `sitemap.xml`.
* Expanded `/teleprompter.html` formatting and saved-script controls: added more reader fonts, font weight, expanded text color swatches, custom text color, reader background palettes, custom reader background color, and a Save as action for keeping multiple browser saved-script versions.
* Added `/av-calculator.html`, a browser based AV calculator with audio delay, projection throw, record storage, and power load checks, saved browser values, copyable operator summary, and text export. Linked it from the homepage AV Tools section, added it to the Products footer, and added it to `sitemap.xml`.
* Added `/cue-sheet.html`, a browser based AV cue sheet for run of show work with starter cues, editable cue rows, selected and next cue controls, status tracking, keyboard shortcuts, print output, JSON import and export, CSV export, and remembered browser state. Linked it from the homepage AV Tools section, added it to the Products footer, and added it to `sitemap.xml`.
* Added `/show-timer.html`, a stage friendly AV show timer with countdown, count up, clock mode, presets, custom duration parsing, quick time adjustments, warning and overrun states, keyboard controls, stage view, and remembered browser preferences. Linked it from the homepage AV Tools section, added it to the Products footer, and added it to `sitemap.xml`.
* Reworded `/widgets.html` public copy to describe current URL parameter configuration without permanence framing and replaced external demo iframes with static preview panels so the page has no third party runtime errors.
* Added a live next cue readout to `/teleprompter.html`: the run HUD and remote now show the next marker, estimated time until cue, and clock time when a show start is set.
* Added show start scheduling to `/teleprompter.html`: operators can set a show start time or use Now, see the planned end time in the run HUD and remote, and view estimated clock times for each marker in the rundown.
* Added a timed marker rundown to `/teleprompter.html`: operators can open a rundown dialog showing each marker's estimated start time, segment duration, line number, progress position, and jump control.
* Added a target pace readout to `/teleprompter.html`: when a target runtime is set, the run HUD and remote now show whether the current speed is on target, fast, or slow against that duration.
* Added target runtime fitting to `/teleprompter.html`: operators can enter a run time such as `02:00`, fit scroll speed to that target, persist the target in saved scripts and show packages, and see it in the script run metadata.
* Added portable show packages to `/teleprompter.html`: operators can export the current script, formatting, and saved-script library to JSON and import it on another browser session with duplicate-safe saved-script merging.
* Added a companion remote control mode to `/teleprompter.html`: the reader window can open a separate remote window for play, cue, restart, speed, marker, step, read mode, and edit mode commands with live reader status sync.
* Added operator marker navigation to `/teleprompter.html`: markdown headings, bracket cues, and divider cues now become jump markers with a marker dropdown, previous and next controls, keyboard shortcuts, and a live marker readout in the run monitor.
* Added pro run controls to `/teleprompter.html`: a 3-second Cue + Play leader, live elapsed and remaining run time, progress percentage, progress rail, and script word-count/run-rate readout.
* Expanded `/teleprompter.html` formatting and script management: added reader font family choices, text color swatches, and a local saved-scripts library with Save, Load, Delete, and per-script formatting snapshots.
* Added lightweight anticipatory UX to `/teleprompter.html`: context-aware next-action highlights, short dynamic hints, smarter title-based Save filenames, last-mode restore, and browser preference metadata in `localStorage`.
* Added an unlisted, noindex **Prompt Browser** page at `/prompt-browser.html` for the full prompt library browser. The page includes ranked search, review queue, fill-in mode, saved views, import/export state, and browser state. It is blocked in `robots.txt` and kept out of `sitemap.xml` and site navigation because the embedded library includes internal working prompts.
* Added tactile microinteractions to `/teleprompter.html`: pressed and hover button states, control confirmation pulses, inline file save/load success and error feedback, paused-reader drag resistance with snap release, and reduced-motion fallbacks.

## 2026-06-12

* Added `/teleprompter.html`, a self contained teleprompter page with editable script text, smooth auto scrolling, keyboard controls, mirror mode, fullscreen, manual save and load file controls, and a starter sample script for first load.

## 2026-06-11

* Added a public-safe **Project Registry** at `/project-registry.html` with searchable/filterable metadata for 52 public DaveHomeAssist repositories. Private repos, local filesystem paths, dirty worktree counts, and internal triage notes remain excluded from the published data set. Added the page to the homepage Resources footer and `sitemap.xml`.
* Launched a public **Prompt Library** browser at `/prompts/` — a self-contained, zero-dependency page (vanilla JS) with 228 curated, copy-ready prompts: instant search, faceted filters (collection/area/pack/format/platform), and one-click copy. Public-safe subset (internal/operational prompts, Notion IDs, and flagged drafts excluded). Added to `sitemap.xml`.
* Upgraded `/prompts/` with five features (smoke-tested headless, zero console errors): `{{variable}}` fill-in with live preview + remembered values, relevance-ranked search with match highlighting, per-prompt permalinks (`#p=id`) + shareable view-links, `j`/`k` result navigation plus a ⌘/Ctrl-K command palette, and full keyboard/ARIA accessibility (live regions, focus-visible rings, reduced-motion). Plus per-group counts in the facet dropdowns.
* Linked `/prompts/` (nav + Products footer) and `/profile/` (Connect footer) from the homepage so both routes are reachable, not just listed in `sitemap.xml`.

* Added a private, unlisted prototype page at `/youtube-pipeline.html` for the Notion based YouTube Pipeline. The page is marked `noindex, nofollow`, blocked in `robots.txt`, and kept out of `sitemap.xml` because it still requires the Claude artifact host APIs for live data and writes.
* Added `youtube-pipeline-regression-2026-06-11.js` plus `youtube-pipeline-production-plan-2026-06-11.md` beside the prototype so the data loss guard and remaining production gates are visible in repo.
* Hardened `/youtube-pipeline.html` for public static hosting: added host capability gating, explicit database ID validation, safer local draft storage, a clear local drafts action, and MCP request timeouts.

## 2026-06-10

- Added a private, unlisted sub-page at `/flooring-contract-review.html` — a plain-English review of the Robertson family flooring contract (12 priority-ranked issues, annotated full-contract view, ready-to-send email). Self-contained page (own inline styles/JS, system fonts).
- Marked the page `noindex, nofollow` and added a `robots.txt` Disallow; kept it out of `sitemap.xml` and the site nav, since it contains personal details and is meant to be shared by link only.
- Content update on `/flooring-contract-review.html` after new info: the selected floor is now identified (EF Legendary / Dreamweaver, color 6005 Orthodox, 5.2 mm / 20 mil SPC). Reframed Issue 2 (and annotated comment #2) from "product missing" to "identified, but reconcile the spec" and flagged the contract's 22 mil vs the product's 20 mil. Added a "Selected floor" header tile, a manufacturer-warranty note to Issue 4, a "Second opinion / alternate bid" card (Getz estimate Fri Jun 12), and a material-vs-labor split (≈$2.59 material / ≈$3.85 labor of the $6.44/sq ft) to the pricing section. Edits made in both views; toggle/filter/copy and print/mobile styles unchanged.

## 2026-05-05

- Added the Davai memory architecture static reference bundle at `/systembydave/`, including seven layer pages, routing, lifecycle, INDEX.md, sync, voice, and implementation guides.
- Added generated Davai diagrams, storyboard scripts, a standalone upload zip, and `build_site.py` for rebuilding the bundle.
- Added `/systembydave/` URLs to the root sitemap and advertised the section sitemap from `robots.txt`.

## 2026-05-04

- Added a public resume page at `/resume/` with System by Dave styling, a signal-flow visual, responsive layout, print styles, and reduced-motion support.
- Fixed self-hosted font URLs in `css/fonts.css` so pages load fonts from the root `fonts/` directory.
- Added the resume route to `sitemap.xml`.

## 2026-03-31

- Replaced the `widgets.html` preview placeholders with live lazy-loaded iframe embeds for Project Status Dashboard, Quest Log, Client Approval Hub, and Workspace Map.
- Adjusted the homepage `Sort Inbox` product card image crop to anchor the artwork at the top of the frame so the illustration is not clipped.
- Verified the updated homepage and widgets page were published successfully on GitHub Pages.
